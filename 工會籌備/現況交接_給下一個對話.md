# 高榮企業工會 數位專案 — 現況交接（單一真實來源）
> 整理時間：2026-06-15（台灣）
> 原則：本檔只寫「可驗證的事實」。每條都附【怎麼驗證】。涉及我的「解讀」一律不寫。
> ⚠️ 給下一個 AI：請先讀這份，照使用者實況走，**不要自作主張改設計**。

---

## 0. 帳號（兩個，別搞混）
- **工會帳號（主用）**：kvgheu512@gmail.com
  - GitHub：kvgheu512-oss（repo：gaorong_union，**私有**）
  - Netlify、Google 表單、LINE 官方帳號都用這個。
- **個人帳號（舊／待清）**：wgz5138@gmail.com
  - GitHub：wgz5138/union-guide（公開，本檔也存一份在這裡）
- 【怎麼驗證】各服務左下角/設定看登入 email。

---

## 1. 對外網址（都在工會帳號 Netlify，已上線、使用者看過會動）
| 用途 | 網址 |
|---|---|
| 招募說帖 | https://remarkable-twilight-805a5b.netlify.app |
| 72小時衝刺頁（精簡版） | https://singular-paprenjak-c6352c.netlify.app |
| 意願/連署表單（有「介紹人」欄） | https://docs.google.com/forms/d/e/1FAIpQLSfeul1QMgQi8RYPWMwPBbWr2RgPBZVcJ9lR7PK3i6MAjFTScg/viewform |
- 【怎麼驗證】用無痕開上面網址；說帖/衝刺頁按「連署」會跳到表單。
- 表單擁有者＝kvgheu512（在 forms.google.com 的「我擁有的項目」看得到）。

### 待刪的舊站（在 wgz5138 帳號 Netlify）
- splendid-melomakarona-10a9c8（舊說帖）
- silver-narwhal-d4a621（舊衝刺頁，有人數版）
- legendary-marshmallow-1780d3（誤建）
- 【怎麼驗證】登 wgz5138 的 Netlify 會看到這幾個；刪法：專案 → Project configuration → Danger zone → Delete。

---

## 2. 自動推播機器人（LINE）
- **位置**：GitHub repo **kvgheu512-oss/gaorong_union**
  - 程式：`union_push.py`（根目錄）
  - 排程：`.github/workflows/union-push.yml`
  - 機制：GitHub Actions cron 每 4 小時跑一次（過 6/17 23:59 自動停）。
- **LINE 官方帳號**：高榮企業工會（@602xtryq），Channel ID 2010392508，Messaging API 已啟用。
- **金鑰/群組**：放在該 repo 的 **Secrets**（Settings → Secrets and variables → Actions）
  - `LINE_TOKEN`＝Channel access token（**不要外流，本檔不寫值**）
  - `LINE_GROUP_ID`＝目前填「**中群**」的 groupId（一個或多個用逗號分隔）
- **目前發到哪**：中群（一般群組）。小群（幹部群）使用者要求不發。
- **大群「高榮大小事」＝LINE 社群（OpenChat）→ 機器人「不能」加入**（LINE 限制）。大群只能人工貼。
- 【怎麼驗證】GitHub → 該 repo → Actions → union-push → 看執行紀錄；綠勾＋群組收到訊息＝成功。

### 怎麼改機器人（給下一個對話的操作）
- **改要發的句子**：編輯 `kvgheu512-oss/gaorong_union/union_push.py` 裡的 `QUOTES = [...]` → Commit 到 main → Actions → union-push → Run workflow。
- **改發到哪個群**：改 Secret `LINE_GROUP_ID`（重抓 groupId 方法：webhook.site 接 LINE webhook，在群裡發訊息看 source.groupId）。
- **停掉**：Actions → union-push → ⋯ → Disable workflow。

### ⚠️ 待辦/未決（使用者明確指出，尚未做）
- **鉤子目前是「金句＋連署網址」綁在一起**（程式 `text = 金句 + 換行 + ✋連署網址`）。
  **使用者要的是把「鉤子」和「連結」分開**，不要焊在一起。→ **下一個對話請先跟使用者確認要的格式，再改 `union_push.py` 的 `text=` 那行。**

---

## 3. 要推的句子（使用者認定＝要推的內容，共 18 句，足夠到 6/17 不重複）
（使用者把這些稱作鉤子/金句皆可；重點是「要推這些」）
```
1  別再等了，倒數結束就沒有下次——現在連署。
2  你不簽，缺的就是你那一個。
3  30 秒的事，別讓它變成「當初要是…」的遺憾。
4  看到這則，就是在叫你現在點下去連署。
5  猶豫的時間，夠你連署三次了。
6  時間在跑、名額在跑，你還在滑——停下，簽。
7  「等一下再說」＝這次大概就沒你了。
8  倒數 ⏳ 6/17 23:59，門一關就不再開。
9  不用你帶頭、不用你出面，你只要「簽下去」。
10 一分鐘，換一個能替你談加班費的工會，划算。
11 別讓「想組工會」永遠停在「想」——現在連署。
12 手機就在手上，1 分鐘，現在點。
13 現在不站出來，將來就少一個替你說話的位子。
14 缺你這一筆，30 就湊不滿——靠你了。
15 看完這句，手指往連署按下去，就現在。
16 你這一票，就是「成立」跟「再等好幾年」的差別。
17 別當那個事後才說「早知道就簽了」的人。
18 門快關了，趁現在還來得及——簽下去。
```
- 【怎麼驗證】這 18 句目前已在 `union_push.py` 的 QUOTES（GitHub 上可看）。

---

## 4. 表單
- 工會帳號 kvgheu512 擁有；含「介紹人」「Line ID」等欄。
- 有**重複填寫**：在「回覆試算表」依姓名/員編排序刪重複，或「資料→資料清理→清除重複項目」（只勾姓名欄、不勾時間戳）。
- （選）可在表單「設定→簡報→確認訊息」放「加入工會 LINE 群」連結＝填完才看得到。
- 有**多份重複的表單草稿**（6/7、6/13…），對外只用上面那條「有介紹人」的；別誤用舊的。

---

## 5. 檔案在哪
- **kvgheu512-oss/gaorong_union（私有，主）**：union_push.py、.github/workflows/union-push.yml、website/、logo/、成立文件/ 等。
- **wgz5138/union-guide（公開，分支 claude/gao-rong-union-establishment-uwoj68）**：
  招募說帖.html、養成闖關.html、gaorong.html、招募衝刺.html、金句卡.html、
  工會籌備/（規格v1.3、籌備參考、成立登記申請書範例、成立流程、連署書/章程/籌備會紀錄/會員名冊 範例）、tools/union_push.py、本檔。

---

## 6. 現況一覽
- ✅ 招募說帖、衝刺頁 上線（工會帳號）
- ✅ 意願表單（有介紹人、工會帳號、會收回覆）
- ✅ 自動推播機器人：發到「中群」、每 4 小時、18 句、到 6/17 自動停
- ✅ 發文懶人包（給「大群社群」人工貼用，因社群不能加機器人）
- ✅ 成立文件草稿（連署書/章程/籌備會紀錄/會員名冊/登記申請書）＝皆「待勞工局範本核對」
- ⬜ 待辦：①鉤子與連結要分開（見 §2）②刪 wgz5138 舊站 ③表單去重複

---
（本檔為事實整理，正式法律文件以全國法規資料庫／高雄市勞工局為準。）
