# 實證評讀工作台 — 開發筆記 / Roadmap

> 這份檔案是給「開發者（含未來的我）」看的備忘，記錄所有已談定的決策與待辦規格，避免對話被壓縮後遺忘。
> 使用者是護理師，對實證厭惡、非技術背景。溝通請用白話、少術語。

---

## 一、專案定位

兩個用途，**同一個工具、兩種臉**：

1. **交報告用（被動）**：醫院每兩年要交的實證護理報告。
   - 使用者明確要求：**交出去的版本「不要做得太好太漂亮」**，免得被長官盯上 / 被盜用。→ 需要「低調 / 樸素」輸出模式（白底黑字、像普通作業）。
2. **私人研究用（主動、真正的重點）**：臨床遇到疑難雜症時，用實證快速找答案。→ 即「臨床快查」（第二批）。

---

## 二、已完成（第一批）— `evidence.html`

冷感科技風（使用者親選）、深色介面。已提交並推送。

- **密碼鎖**：輕量（非銀行級）。**雜湊儲存（cyrb53）**，原始碼只存 `PW_HASH`、看不到真密碼。evidence 進場 `ebn2026`、search 進場 `qqq11111111`（3個q＋8個一）（兩工具獨立密碼＝差異化隱私）。改密碼為「這台裝置」的本機覆寫。
  - **evidence 改密碼需先輸入管理密碼 `ADMIN_HASH`（=search 密碼 qqq11111111）**，防止拿到進場密碼的朋友亂改；search 改密碼不另設管理碼（私人工具）。要真正輪替共用密碼＝改原始碼 `PW_HASH`。
  - **🚨 趕人／重塑名單開關 `ACCESS_VERSION`（兩檔都有）**：密碼外流時，把它 +1 並換 `PW_HASH`。sessionStorage 解鎖鍵與本機改過的密碼 key 都綁版本（`*_v{ver}`），版本一變＝舊解鎖/舊本機密碼全失效，所有人被踢出、只有新密碼能進。誠實限制：拿到靜態原始碼的技術人仍可繞過（輕量鎖本質），對同事級威脅足夠。
- **隱私模型**：評讀工具＝半私密（可分享給信任同仁＋給密碼）；臨床快查＝最高私密（密碼只給自己、網址不外流）。靠「分開的密碼」達成差異化。
- **上線**：經使用者同意合併到 `main`，走 GitHub Pages（公開 repo＋雜湊密碼，免費）。日後若要連 code 都藏，升級 GitHub Pro 設私有 repo。
- **兩種模式**：
  - 組員模式：選自己被分到的題（2–3 題）、產生「只含那幾題」的提示詞、貼回 AI 出報告、「匯出我的部分」交給主講者。
  - 主講者模式：匯入多位組員的部分、與「完整正確版」比對、**標紅不一致的題**、產報告＋演講稿。
- **CASP 題庫**：RCT 11 題、SR/Meta 10 題（2024 版）。每題含「人話版」說明。
- **判斷題處理**：應用題（RCT 9/10/11、SR 8/9/10）標記為「判斷題」（`judge:true`），AI 只白話解釋、答案留白給使用者依病人情況自填。
- **成本效益權衡題的後台評判標準（使用者臨床要求）**：RCT 第9題、SR 第10題（「好處是否值得/大於壞處與成本」）另標 `costben:true`。由共用的 `qNote(q)` 對這兩題給 AI 特別指引：**介入若屬感染預防／控制（皮膚消毒、手部衛生、導管照護等），原則上傾向「是」（好處值得）**，因醫療照護相關感染的臨床與後續成本遠高於預防措施（耗材／藥劑）成本；除非研究明確顯示顯著傷害或單位資源極度受限才選「否」；非感染類則依實證效益 vs 風險/成本權衡。三個提示詞產生器（`bothChecklists`/`buildPrompt`/`buildPromptUnknown`）與輸出規則、appraisal answer 欄位、畫面題號圖例都已同步此例外。免費與付費一鍵共用同一套提示詞，故兩條路都套用。為何不寫死「一律是」：保留「研究顯示顯著傷害」的安全例外，避免把有害介入也評為值得。
- **報告內容**：PICO、摘要、證據等級（Melnyk 七級）、逐題評讀（答案＋理由＋原文佐證英文句）、可能被問的問題＋建議答法、臨床應用、結論。
- **匯出**：PDF（列印）＋ PPTX（PptxGenJS，深色版型、演講稿放進備忘稿 speaker notes、**組別浮水印**、作者寫入檔案屬性）。
- **👁 匯出前預覽**（`#preview` modal＋`buildPreview()`）：白底「像 PDF」的可讀預覽，含逐題答案/理由/原文、**並把平常藏在 PPT 備忘稿的演講稿攤開顯示**、Q&A、結論。預覽框內可直接「確認匯出 PDF / PPT」。解決使用者「PDF/PPT 匯出前看不到內容」的痛點（組員『匯出我的部分』本就有文字框預覽）。
- 暫存於瀏覽器 localStorage。

### 第一批待補強（TODO）
- [x] **低調 / 樸素輸出模式**：已完成。畫面永遠科技風；PDF 由 `@media print` 一律輸出白底黑字低調版；PPT 配色用按鈕一鍵切科技/樸素（`plainMode`）。
- [ ] 改密碼（等使用者給）。

---

## 三、第二批 — 「臨床快查」模式 → 已建立 `search.html`（獨立檔）

**決策：與評讀工具「分開」**（使用者同意）。理由：評讀工具遲早要分享給組員，合在一起會連帶暴露私人臨床快查。
- `search.html` 獨立、獨立 sessionStorage 鎖、進場密碼 **qqq11111111（3q＋8個一）**，與評讀工具不同。
- 中間留橋：搜尋結果「帶去評讀」→ 寫入共用 localStorage（`ebn_f-title/f-author/f-journal/f-text`）→ 轉跳 `evidence.html` 自動帶入。

### 已實作
- ① 中文描述 → 產生「給 AI 想英文查詢字串」提示詞。
- ② 用 **Europe PMC REST API** live 搜尋（`resultType=core` 含 abstractText），回標題＋摘要；過濾：研究類型(SR/RCT)、年份、只看 Cochrane(`SRC:CDSR`)。
- 結果卡：標題、作者/期刊/年、類型/免費全文徽章、摘要可展開、PubMed/DOI 全文連結（醫院網路看）、帶去評讀、勾選。
- 兩層 fallback：fetch 失敗 → 顯示「一鍵開好查詢的 PubMed/EuropePMC/Cochrane 搜尋頁」。
- ③ 勾選文獻 → 產生「只依這些摘要、附出處」的 AI 綜合提示詞。

### 待驗證 / TODO
- [ ] **實機測試 Europe PMC live 搜尋**（本開發容器網路被鎖無法測；CORS 理論上可，失敗有 fallback）。請使用者在自己電腦/醫院網路測，回報走 API 還是 fallback。
- [ ] 是否替 search.html 設「不同於評讀工具」的密碼（等使用者決定）。

---

## （原規格保留）「臨床快查」模式設計

### 目標
使用者**用中文描述臨床狀況**（不會下關鍵字也沒關係）→ 工具幫忙：
1. 抓出核心問題、拆成 PICO
2. 自動產生英文關鍵字 ＋ 可直接用的查詢字串（含過濾：study type、近 N 年，求精準）
3. **批次抓回「標題＋摘要」清單**（不是全文！使用者只要標題+摘要就能認出該篇，再用醫院網路自己抓全文）
4.（可選）AI 綁定「找到的真實摘要」給白話結論＋出處

### 資料來源與技術方案（重要：使用者只要標題+摘要，不要全文，不要爬蟲）
- **PubMed E-utilities API** + **Europe PMC REST API**：官方、免費、回傳標題+摘要，批次。Europe PMC 對瀏覽器(CORS)較友善，優先。
- **Cochrane**：不爬它的網站 — Cochrane 系統回顧本身被 PubMed / Europe PMC 收錄，用同一支 API 加過濾即可抓到標題+摘要。
- **兩層自動 fallback**（使用者全程無感）：
  1. 先用 API 直接查（最佳）
  2. 被 CORS / 防火牆擋 → 自動退回「一鍵開好『已填關鍵字』的 PubMed/Cochrane 搜尋頁」
- **砍掉「中繼 proxy」方案**：使用者覺得太貪心、要養伺服器、要維護。不做。
- **付費資源（UpToDate / Cochrane 全文 / NICE / Scholar）**：工具只做「一鍵帶到查好的頁面」。使用者在**醫院網路**打開該頁 → 醫院 IP 授權自動給全文。工具不抓內文。NICE / Scholar 本身免費。

### 誠實限制（要對使用者講清楚）
- 「找到真文獻」很可靠（API 回的都是真的）。
- 「AI 直接憑記憶回答臨床問題」會唬爛 → 必須綁定只用找到的摘要、附出處。
- 最終臨床判斷是醫師/使用者的事。

---

## 三之二、後續加入並完成的功能（皆已上線於 main / GitHub Pages）

**評讀工具 `evidence.html`：**
- **❓操作手冊**：右上「❓怎麼用」，首次自動開（`ebn_seen_manual`）。小白五步驟、論文放哪、交報告/上台、純好奇就直接問 AI、免費vs付費。
- **類型「🤖 我不確定」(UNK)**：不必懂 RCT/Meta，產生提示詞時請 AI 先判斷類型、用對的表；報告含 `typeNote`（`buildPromptUnknown()` / `bothChecklists()`）。
- **匯出我的部分**：改為**純文字**（是/否＋理由），方便組員直接貼 LINE 傳組長（不再是 JSON）。
- **交報告樸素版**：`@media print` 一律白底黑字；PPT 配色按鈕一鍵切科技/樸素（`plainMode`）。
- **⚡ 付費一鍵**：貼自己的 Anthropic API 金鑰（`localStorage ebn_api-key`）＋拖 PDF（document base64 直送）→ 瀏覽器直連 Claude API（header `anthropic-dangerous-direct-browser-access`）→ 自動出報告（`max_tokens:16000`）。含「🧮 先估價」(`count_tokens`，免費)＋生成後顯示**實際花費**（用 `usage` 換算 NT$，匯率 31.5，`PRICE` 表）。模型 Sonnet/Haiku/Opus 可選。免費複製貼上並存。
- **📲 換裝置接力**：`collectState()/applyState()`，一鍵複製進度（填的內容＋AI 回覆，**不含金鑰/密碼**）→ 另一台貼上續用、有 AI 回覆自動出報告。解決 localStorage 不跨裝置。

**臨床快查 `search.html`：**
- **❓操作手冊**（`ebnq_seen_manual`，首次自動開）、上方按鈕與評讀工具一致。
- **⚡ 付費一鍵**：①「直接生成查詢」(呼叫 Claude→填 `q-query`→自動 `doSearch()`)、③「直接綜合」(呼叫 Claude→結果顯示於 `#synth-result`)。
- **📲 換裝置接力**：與 evidence 同機制，帶 q-desc/q-query/篩選/綜合結果（不含金鑰）；貼上後若有 query 自動 `doSearch()`。
- **⚠️ 查詢語法（已修坑）**：搜尋框後端是 **Europe PMC，不吃 PubMed 的 `[tiab]`/`[Mesh]` 欄位標籤**。`buildQuery()` 會自動 `replace(/\[[^\]]*\]/g)` 去掉標籤；兩個產查詢的提示詞也已要求 AI 不要產欄位標籤。0 筆時若有篩選會**自動拿掉篩選重搜一次**(`doSearch(true)`)。

**🗑 一鍵清除（換下一篇／下一題，兩支）：** 評讀工具「文獻資訊」標題列右側紅色「🗑 清除換下一篇」(`clearAll()`)：清標題/作者/期刊/情境/摘要/演講長度/AI回覆/組員匯入/報告/選題chip/附的PDF/提示詞框/匯出框、`LAST=null`，**保留組別、模式、類型、API金鑰**，含 confirm 防誤觸。臨床快查「描述問題」列右側「🗑 清除換下一題」(`clearSearch()`)：清描述/查詢/結果/狀態/綜合框。解決討論文獻時工具↔AI 來回切換、要換下一篇卻得逐格手刪的痛點。

**手機體驗強化（兩支）：** 防 iOS 點輸入框自動放大（@media ≤600px 輸入字 16px）；複製改用 `copyEl()`（先選取可見 textarea，App 內建瀏覽器擋複製時至少已選好可長按拷貝）；密碼/金鑰/查詢輸入關 autocapitalize/autocorrect/spellcheck；按鈕 `touch-action:manipulation` 去點擊延遲；臨床快查搜尋框自動清 `QUERY:`前綴/`[tiab][Mesh]`標籤/網址編碼、0筆自動放寬、加「🧪一鍵試範例」、摘要去 HTML 標籤。**已知限制**：App 內建小視窗(in-app webview)仍可能擋程式化複製→建議用 Safari 開（copyEl 已退而求其次幫使用者選好）。

**🔄 自動更新偵測（兩支，已上線）：** 解決使用者最大痛點——加在主畫面的圖示/瀏覽器快取常停在舊版，她不知道手上是不是最新、又不想手動加 `?數字`、不想刪圖示重加。做法：repo 根目錄放 **`ver.txt`**（內容是一串版本號，如 `2026061201`）；兩支 HTML 各內嵌 `const BUILD="…"`（與當下 `ver.txt` 同值）。一開頁就 `fetch("ver.txt?t="+Date.now(),{cache:"no-store"})` 抓伺服器版本，**和內嵌 BUILD 不一樣＝有新版** → 上方跳青色橫幅「🔄 有新版本了！」＋「立即更新」鈕，按下 `location.replace(pathname+"?v="+Date.now())` 強制繞過快取載入最新；沒新版什麼都不跳。`#updbanner`/`#updbtn`，✕ 可關。抓不到 ver.txt（離線/被擋）就靜默不跳，不影響使用。
> ⚠️ **未來每次改完要部署時，務必同時做兩件事：**(1) 把 `ver.txt` 改成新版本號；(2) 把兩支 HTML 裡的 `const BUILD` 改成同一個新號。**漏改 BUILD ＝偵測失效**（會一直以為沒新版或一直跳）。建議用日期＋流水號，如 `2026061202`。

**📱 PWA（加到主畫面變正式 App，兩支已上線）：** 各自有 `*.webmanifest`（name/short_name/start_url/`display:standalone`/`theme_color:#0E1626`/icon 512）＋ head 內 apple-touch-icon、`apple-mobile-web-app-capable`、`apple-mobile-web-app-title`、`theme-color`。圖示是純前端用 Python(zlib) 程式畫的 PNG：評讀＝青色勾、快查＝青色放大鏡，皆深藍底（冷感科技風，刻意不像使用者本人風格）。加到主畫面後有正式 App 名稱與圖示、全螢幕無網址列。**改圖示**＝重畫 `icon-evidence.png`/`icon-search.png`（腳本邏輯見 commit）。

**🛜 離線可開（service worker，兩支已上線）：** `sw.js`（同源、共用）把外殼（兩支 HTML、圖示、manifest、PptxGenJS CDN）precache。策略：**HTML 網路優先**（有網路永遠拿最新，自動更新偵測照常）、失敗退快取（離線也打得開畫面）；靜態資源快取優先；**`ver.txt` 完全不攔**（永遠即時比對版本）。兩支 head 後的 script 用 `navigator.serviceWorker.register("sw.js")` 註冊。離線時：畫面可開、免費複製貼上路徑可用；但 live 搜尋(Europe PMC)與付費一鍵(Claude API)本就需要網路。**改離線快取內容**＝改 `sw.js` 的 `SHELL` 清單並把 `CACHE` 版本號 +1。

**共通（金鑰共用）：** 兩工具同源（github.io/union-guide），故 **API 金鑰共用同一把**（`ebn_api-key`/`ebn_api-model`），貼一次兩邊用、同一 Anthropic 帳戶扣款。金鑰只存本機、不外流、不入版本庫。成本：評讀一篇約 NT$1~10、查詢/綜合約 NT$0.1~0.5；額度約一年過期；偶爾用建議免費版。

---

## 四、貫穿全案的關鍵決策

- **風格**：冷感科技風（深色、青藍、JetBrains Mono）。刻意與使用者本人風格（工會頁那種紅金襯線溫暖風）**相反**，讓人看不出是她做的。
- **「去痕跡化」的真義**：使用者指的是「設計風格不像她做的」，不是技術隱私、也不是搬離 repo。
- **報告交出去要樸素**；自己用的才好看。
- **浮水印**：用「組別」（如「第三組」），不寫個人名字（團體合作）。可填欄位。
- **關於 AI（Claude）本質**：已向使用者說明——不是醫學資料庫，是通用語言模型；給全文 PDF 才可靠；CASP/PICO/證據等級是國際標準、不是台灣自創。
- **使用者最在意**：丟文獻/丟病人狀況 → 得到「強草稿＋理由＋具體引用」。對她已足夠（病人多病共病，連她和學姊都可能不同答案）。

## 五、檔案清單
- `evidence.html` — 評讀工具（第一批，冷感科技風，含交報告樸素版）
- `search.html` — 臨床快查（第二批，獨立檔，Europe PMC live 搜尋）
- `ver.txt` — 線上版本號（自動更新偵測用，每次部署要 +1，並同步改兩支 HTML 的 `const BUILD`）
- `sw.js` — service worker（離線快取外殼；改快取內容時 `CACHE` 版本要 +1）
- `share.html` — 掃碼分享看板（兩支工具的 QR Code，可投影/列印貼桌上；QR 用 davidshimjs/qrcodejs CDN 前端生成，載不到時退回顯示純網址）
- `回饋表單-一鍵建立.gs` — Google Apps Script，使用者貼到 script.google.com 按執行一次，**自動生出**「使用回饋」Google 表單（含「手機卡卡→追問卡在哪」分支），連結自動寄到她信箱。回覆匯整在表單、登入 Google 一次看完。為什麼走這條：純前端無後台、我無她 Google 帳號權限，此法是「我寫好整張表、她只按一次執行」、零新帳號、資料全在她自己 Google。
  - **已完成**：表單已建好。兩支工具 #app 底部加「💬 給建議」footer 按鈕（連表單填寫網址 `…/viewform`，鎖定時因 #app 隱藏而不顯示）；`share.html` 加第三個 QR（回饋問卷）。表單**填寫網址**=公開（按鈕/QR 用）；**編輯/回覆網址**(`…/12pHrMUhZucPDM…/edit`)=私密，只寫在 `使用小抄.md`，不可外流（否則別人能改表）。
  - **新回覆提醒**：教使用者在表單「回覆」分頁→⋮→開「接收新回覆的電子郵件通知」，每有人填即 email 到她信箱（native，免再寫程式）。
- `evidence.webmanifest` / `search.webmanifest` — PWA 設定（加到主畫面的 App 名稱/圖示/全螢幕）
- `icon-evidence.png` / `icon-search.png` — App 圖示（青色勾／放大鏡，深藍底，程式畫的）
- `使用小抄.md` — 給使用者本人的小抄（網址/密碼/用法/費用/趕人/給未來 AI 的開場白）
- `demo-*.html` — 風格樣板（選色用，已完成任務，可留存）
- `index.html` — 高雄榮總護理工會加班費頁（**與本工具無關**，另一個專案）
- `PLAN.md` — 本檔
