# 🚚 工會網站搬家筆記（私人帳號 → 工會帳號）

> 目的：讓工會的東西都歸**工會帳號 `kvgheu512-oss`**，跟你的私人帳號 `wgz5138` 完全切開。
> 目前：先不搬，晚點再做。這份筆記提醒你「要帶什麼、怎麼做」。

## ✅ 要搬去工會帳號的檔（其餘一律不帶）
- `gaorong.html` + `gaorong.webmanifest`（工會網站）
- `union.html` + `union.webmanifest`（幹部功能表）
- `id-mark.html` + `idmark.webmanifest`（證件工具）
- `finance.html` + `finance-law.js` + `finance.webmanifest` + `laws.json`（透明帳本）
- `receipt.html` + `receipt.webmanifest`（單據快拍）
- `privacy.html`（隱私政策）
- `sw.js`（離線）、`assets/`（會徽與 App 圖示）

## ❌ 不帶（個人／其他專案，留在原處）
- `freelance-flights-mvp/`（接案：報價單、應徵信、作品集）
- `demo-corp*.html`、`demo-tech.html`、`style-demo.html`（demo）
- `lottery.html`（威力彩選號器）、`回饋表單-一鍵建立.gs`（實證護理工具的回饋表單）
- 法律工具 `evidence/lawyer/search/share` 等（要的話之後再加）
- `index.html`、`PLAN.md`、`README.md`、`使用小抄.md`（含個人／開發資訊）

## 🪜 搬家步驟（用「工會帳號」操作）
1. 跟 Claude 說「**幫我打包工會檔**」→ 拿到一包乾淨 zip（無個資、無歷史）。
2. 登入**工會帳號** → 新建一個**空 repo**（例如 `union-guide`，**不要用 fork**）。
3. 該 repo →「Add file → Upload files」→ 把 zip 解開後的檔案拖進去 → Commit。
   - 這樣提交作者＝工會帳號，**沒有 `wgz5138`、沒有「forked from」**。
4. Settings → Pages → 由 `main` 發布。新網址：`kvgheu512-oss.github.io/union-guide`。
5. 刪掉工會帳號目前那個 **fork**（`kvgheu512-oss/union-guide` 舊的）。
6. 把所有發出去的連結換成新網址。
7. （之後）私人帳號這個 repo 設為私有或刪除。

## 🤖 讓 Claude 之後也能幫你改工會帳號
搬完後，在 claude.ai/code 把**工會帳號的 repo 也連接／授權**一次，之後就能像現在這樣直接幫你改工會帳號裡的東西（參考 code.claude.com/docs）。

## ⚠️ 另記：私人 repo 是「公開」的
目前 `wgz5138/union-guide` 是公開的，裡面還有你的接案資料與其他專案。哪天想處理，可考慮把它設為私有。
