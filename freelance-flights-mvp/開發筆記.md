# 開發筆記 / 交接備忘（給未來的我看，避免失憶後憑記憶亂講）

> ⚠️ 若對話被壓縮或重開，**先讀這份**，不要憑記憶說話。不確定的事就查證或看程式碼。

## 🆕 開新對話時，直接貼這段當開場白

> 先讀 `freelance-flights-mvp/開發筆記.md`，別憑記憶說話，不確定的事就查程式碼或查證。
>
> 我是佳容，做的是機票監測案（A2310，已經確認被其他學員接走，現在 Telegram bot
> 留著自己用，持續優化）。專案在 `github.com/wgz5138/union-guide`，`main` 和
> `claude/jie-an-ugvafg` 分支都有最新程式碼，本機在 Windows
> `F:\機票小幫手\freelance-flights-mvp`。
>
> 目前狀態：
> - Google Flights 爬蟲（`gflights_scraper.py`）、API 版
>   （`travelpayouts_flights.py`）、Telegram 雙向 bot（`telegram_bot.py`）
>   都已完成、實測、bug 盤查過（含安全審查、code-review）
> - 查詢不用打年月，不填自動查未來 3 個月最便宜（見第 3 節「月份彈性查詢」）
> - Telegram 傳「探索」不用填目的地，掃一輪常查的國家，優先秀降價幅度大的
>   （見第 3 節「探索模式」）
> - 剛做完：每日排程現在不管有沒有降價，都會推一則「機票日報」總結
>   （時間戳＋每條航線現價），解決「快取資料常常好幾天沒變、
>   notify() 規則讓 Telegram 安靜到像壞掉」的問題（見第 3 節「每日總結」）
> - 全部 `.bat` 檔案已改用 Big5(cp950) 編碼，解決繁體中文 Windows 亂碼問題

## 📁 使用者的專案資料夾在哪裡（常忘記，先看這行）
```
F:\機票小幫手\freelance-flights-mvp
```
（Windows 電腦，F 槽。之前誤放過 `C:\Windows\System32`，寫檔會 PermissionError，已搬到這裡。）
使用者常見開場白：「檔案在哪」「要怎麼跑」→ 直接回這個路徑 + 對應 `.bat` 檔名即可，見第 1、3 節。

## 0. 使用者與溝通原則
- 使用者剛學爬蟲、準備接案，**非資深工程師**：請用白話、少術語、一步一步。
- **要誠實、不可誇大**（做到標 ✅、沒做標 🔧）；**不可憑記憶亂報**，需查證（用 WebSearch/看程式）。
- 在 GitHub 平台接案（TeachCake / casecake@teachcake.com），目標案件 **A2310 機票監測**。

## 1. 環境
- 本機：**Windows**，專案在 `F:\機票小幫手\freelance-flights-mvp`（之前誤放 System32 會 PermissionError，已搬 F 槽）。
- 我（雲端沙盒）**不能連外網、不能開瀏覽器** → 所有 Google Flights / 實連測試都要**使用者在自己電腦跑**、回報，我據此修。
- 分支 `claude/jie-an-ugvafg`；**已合併進 `main`**（雲端排程需在 main 才會跑）。
- 注意：本機 git pull 偶爾因雲端自動提交 price_state.json 卡住 → 已移除 workflow 的 push 觸發；必要時 `git pull --rebase`。

## 2. 金鑰（已設定，勿寫進 repo）
- 使用者已用 `setx` 永久設定環境變數：`TRAVELPAYOUTS_TOKEN`、`TG_TOKEN`、`TG_CHAT`(=8813524974)。
- GitHub repo Secrets 也已設同三項（給雲端排程用）。
- **金鑰絕不可進版控**；程式一律從環境變數讀。

## 3. 兩套系統現況

### A) API 版（已完成、實測、雲端在跑）
- `travelpayouts_flights.py`：多航線、**中文地名自動轉代碼**（地名對照表）、單程/來回、
  **降價偵測**（`price_state.json` 記上次價，只在變便宜才通知）、Telegram 推播、繁中 Skyscanner 連結。
- **月份彈性查詢（2026-07-19 加）**：route 不填 `"month"` 就自動查
  `next_months(FLEX_MONTHS_AHEAD=3)`（這個月起連續 3 個月，跨年正確）、合併結果。
  緣由：查證不到 Travelpayouts API `departure_at` 留空的官方行為（文件頁 403 打不開），
  不賭未知行為，改在自己這端把月份清單算好、行為 100% 可控可測。
  `ROUTES` 預設已改成不寫死月份（原本 "2026-09" 寫死會在該月過後永遠查不到卻無提示，
  已修正）；來回票目前**不支援**彈性月份（round_trip 只在有 `"return"` 時觸發，
  彈性只做單程，避免去回程月份配對邏輯複雜化）。
- `telegram_bot.py`：**雙向** bot，在 Telegram 打「查 高雄 東京 2026-09」即查（解析很寬鬆），
  **也可以不打月份**（例：「查 高雄 東京」），會自動用上面的彈性月份查詢。
- **探索模式（2026-07-19 加，`explore_deals()`）**：想去別的國家但還沒決定去哪 →
  傳「探索」或「探索 高雄」（不用填目的地），會掃一輪 `ROUTES` 裡設定的那幾個國家
  （改 `ROUTES` 就等於同時改了探索範圍，不用維護第二份清單），
  優先秀「跟上次查到的價格比，降價幅度（%）大」的，沒有降價紀錄的才用
  「價格低到高」補在後面。降價幅度是跟 `price_state.json` 比較，跟每日排程
  **共用同一份記憶檔**——查得越勤，比較基準越準；同一天連續查兩次通常不會
  再顯示降價（第一次查完就把這次價格存成新的「上次」了），這跟其他地方
  「只在變便宜時才通知」的邏輯一致，不是 bug。
  觸發關鍵字見 `telegram_bot.py` 的 `探索關鍵字`（探索/驚喜/隨便看看/有什麼便宜/
  不知道去哪/隨機）；口語變化太大（例：「隨便看看有沒有便宜的」）偶爾會誤判成
  「看不懂的地名」而回覆友善提示，這是自然語言解析的已知限制，不是 bug——
  使用者只要照說明打「探索」或「探索＋地名」就一定會動。
  安全面：跟既有查詢一樣，逐國家 try/except（單一國家失敗不中斷整輪）、
  API 呼叫間仍有 1 秒禮貌延遲、不接受未知地名直接打 API（先擋在 handle() 內）。

- **每日總結（2026-07-20 加，`send_daily_summary()`）**：緣由是使用者回報
  「TG 完全不會推便宜機票」——查 GitHub Actions 實際 log
  （run 29673130764，2026-07-19 04:19 UTC）才發現**不是壞掉**：
  Travelpayouts 資料是快取，常常連續好幾天價格完全沒變（log 顯示
  「最低 7950 TWD（上次 7950），沒變便宜，不通知」），而 `notify()`
  的規則是「只在變便宜時才推」，於是變成安靜到像沒在跑一樣。
  解法：`main()` 迴圈現在不管有沒有觸發 `notify()`，每條航線都記一行
  （價格/查無票價/失敗），跑完不管今天有沒有值得通知的降價，
  **都會另外推一則「🕐 機票日報」總結**（時間戳用 `TW_TZ` 轉成台灣時間，
  因為 GitHub Actions 跑在 UTC；`datetime.now(timezone.utc).astimezone(TW_TZ)`
  這樣寫在雲端/本機都對，不依賴機器本身的時區設定）。
  `notify()` 跟總結共用抽出來的 `_send_telegram()`，沒重複寫送出邏輯。
  單則超過 3800 字會截斷（Telegram 上限 4096），避免炸掉。
  **提醒**：這代表 Telegram 現在每天固定會多收到 1 則「日報」（不只降價才推），
  這是使用者明確要求的行為（「好歹推一下時間」），不是意外增加的推播量。

- **「高雄→重慶」查無票價，是預期行為不是 bug**：`重慶` 不在 `ROUTES`
  預設清單裡（預設只有日/韓/泰/越/港/星 6 國），使用者是在 Telegram
  互動查詢打的。兩岸航線在 Travelpayouts 免費快取資料裡覆蓋率本來就低
  （見第 4 節「Travelpayouts 免費資料 API＝別人最近 48h 查過的快取，
  冷門航線常查無票價」），這條大機率就是查無快取資料，不是程式壞掉。
  想長期盯這條，可以：① 改用「查 高雄 中國」查整個中國最便宜的城市
  （覆蓋率通常比單一城市高）；② 傳「探索」用探索模式順便看有沒有別的
  划算選項；③ 真的想要即時準確資料，只能靠 Google Flights 爬蟲
  （`gflights_scraper.py`），但那支需要本機真實瀏覽器，沒辦法排程到
  雲端幫你盯這條冷門航線。
- `tg_setup.py`：找 Telegram chat id。
- 雲端：`../.github/workflows/flight-prices.yml` 每天 01:00 UTC（台灣 09:00）跑、推 Telegram、提交 price_state.json。
- 本機自動化：`查機票.bat`/`每日查價.bat`/`背景啟動機器人.bat`/`停止機器人.bat`/`開機背景啟動.bat`/`設定每天自動跑.bat`/`取消每天自動跑.bat`。

### B) Google Flights 爬蟲（核心已完成、實測 17 班，已跑多次驗證 100% 成功率）
- `gflights_scraper.py`（**正式版**）：stealth＋**持久設定檔**（`data/.gf_profile`）＋**自動重試到成功**過反爬；
  「靠內容找卡＋正則解析」抓 **票價/轉機次數/起降時間/航空公司/行李額度**；多航線；CSV+JSON；log。
- **三層錯誤隔離**（航班/航線/執行層級皆有 try/except，單點失敗不影響其他，見技術架構文件 4.3）；
  **成功率統計**：每次執行記錄到 `data/gflights_stats.json`（累加式，供交付物B「連續穩定測試報告」）；
  「成功」判定要求「頁面正常＋實際抓到資料」，不會虛報（技術架構文件 4.3.1）。
- 本機自動化：`查Google機票.bat`（手動跑一次）、`設定Google機票每天自動跑.bat`（設定每日排程，
  會問幾點跑，用 `schtasks` 建 Windows 工作排程「Google機票每日抓取」）、`取消Google機票排程.bat`、
  `_Google機票排程執行.bat`（排程用的無 pause 版，不用手動點）。
  ⚠️ Google 需真實瀏覽器（`HEADLESS=False`），排程觸發時會**彈出瀏覽器視窗**，屬正常現象。
- 拆解過程檔（僅供參考/教學紀錄，正式邏輯已整併進 gflights_scraper.py）：
  `gf_step1_open.py`(打得開) → `gf_step2_results.py`(過系統錯誤) → `gf_step3_extract.py`(抓卡) →
  `gf_step4_stealth.py`(過反爬) → `gf_step5_data.py`(解析) → `gf_step6_persist.py`(持久檔穩定) →
  `gf_step7_detail.py`(詳情) → `gf_step8_baggage.py`(行李)。
- A2310 投案/交付文件：`A2310-作品集.md`、`A2310-報價單與SOW.md`、`A2310-應徵信.txt`、
  `A2310-回覆確認信.txt`（先問4個關鍵問題，未直接承接）、`A2310-技術架構文件.md`、
  `A2310-故障排除SOP手冊.md`（交付物A/C的先行準備，不依賴客戶回覆即可完成的部分）。

## 4. 關鍵發現／決策（避免重踩、避免亂講）
- **Travelpayouts 免費資料 API** = 別人最近 48h 搜尋過的**快取** → 冷門航線/遠期日期常「查無票價」（非 bug）。
- Travelpayouts **即時搜尋 API** 門檻：App **MAU ≥ 5 萬**，個人拿不到。
- **LINE Notify 已於 2025/3/31 停用** → 改用 Telegram。
- **Amadeus 免費自助版 2026/7/17 關閉**（amadeus_flights.py 僅留參考）。
- 想「盯特定航線、真實即時、免費」最實際 → **Google Flights 內建「追蹤價格」**（網站設定，非程式）。
- **Google Flights 爬取要點**：
  - `q=Flights from KHH to NRT on 2026-09-15 oneway` 的網址可**預填搜尋**。
  - 會**間歇出現「系統發生錯誤」** → 必須**重試**（正式版 MAX_TRIES=8）。
  - **headless=True 易被擋** → 用 `headless=False`。
  - **stealth（隱藏 webdriver 等）＋ 持久設定檔** 大幅提高成功率（實測有效）。
  - 結果卡無固定 class → 用「內容含 $價格＋小時＋時間」找 `li`，再用正則解析。
  - 時間是中文制（清晨/上午/中午/下午/晚上/凌晨 + h:mm）；轉機寫「直達」或「轉機 N 次」。
  - **詳情頁（點一班後）有「行李」**：例「免費攜帶 1 件手提行李／第 1 件託運行李須加收費用」。
  - **詳情頁沒有「航班號」** → 誠實結論：Google Flights 不提供航班號（需航空官網/GDS）。

## 5. 投案文件（誠實原則）
- ⚠️ **價格是委託方公告的（NT$ 12,000–16,000）；使用者是「承接」此案，不是報價給客戶。**
  `A2310-報價單與SOW.md` 是「承接範圍說明」：在公告預算內講清楚範圍/加購/保固，付款依平台機制。
- 名字目前是占位符 `【你的名字／待填】`（使用者要問老師後再填，**先別自己填本名**）。
- `A2310-作品集.md`：最前面有**法律與責任聲明**；需求逐條對照，**做到標 ✅、沒做標 🔧**。
- `A2310-應徵信.txt`：可寄 casecake@teachcake.com（記得改【你的名字】）。
- 法律自保重點：接案=承攬（民法 §490+）；§511 定作人終止須賠償、§505 報酬、§507 協力義務、
  §493–495 瑕疵擔保（保固範圍要寫明、改版不算免費修）；著作權法 §12（未約定歸受聘人）；
  爬蟲注意 ToS 與刑法 §358–363；先收訂金、走平台金流、書面 SOW。

## 6. 目前進度（A2310 欄位）
- ✅ 票價/轉機/時間/航空：完成、實測。
- ✅ **行李額度**：完成、實測。關鍵：**詳情頁也會間歇「系統發生錯誤」，要比照列表重試**；
  且要**輪詢等「手提行李/託運行李」字樣出現再讀**。已整合進 `gflights_scraper.py`
  （`FETCH_BAGGAGE`、`BAGGAGE_TOP` 控制；逐班 click→重試→parse_baggage→go_back）。
- ⚠️ **航班號**：Google Flights 介面不提供 → 作品集誠實標明，建議航空官網/GDS 補。
- 結論：A2310 欄位除 Google 不給的航班號外**皆可取得並實測**。剩餘屬選配：
  接回 Telegram/排程、headless 背景化、提高大量抓取穩定性（代理 IP）。
- ✅ **Google Flights 每日自動排程**：`設定Google機票每天自動跑.bat` 已在使用者機器上
  實測成功建立並可正常取消，`gflights_scraper.py` 的三層錯誤隔離＋成功率統計
  （`gflights_stats.json`）也已實跑驗證正常（見第 4、6 節）。

## 7. .bat 中文亂碼案例（重要，別重踩）
- 使用者的 Windows 是**繁體中文 Big5 語系**。`.bat` 檔案若存成 UTF-8（不管加不加 BOM、
  用不用 `chcp 65001`），在該機器上會亂碼＋`@echo off`失效（cmd 讀檔案本身不理會
  BOM，仍用系統 Big5 解碼，desync 到全形冒號「：」「；」附近整段解析錯亂）。
- **正確解法**：`.bat` 檔案直接存成 **Big5(`cp950`)**，`chcp 65001` 改 `chcp 950`。
  已對全部 13 支 `.bat` 套用並實測通過（`設定Google機票每天自動跑.bat`／
  `取消Google機票排程.bat` 皆已在使用者機器驗證乾淨無亂碼）。
- 完整排查過程與判斷準則已寫入 `A2310-故障排除SOP手冊.md` 故障6，**未來若又遇到
  bat 亂碼，先看那一段，別重新從頭猜**。
- 教訓：診斷時曾因「使用者測試時剛好還沒 pull 到最新版」而誤以為修復無效，
  白繞一輪——**以後每次請使用者測試前，務必先明確提醒 `git pull`**，
  並用 `git log -1 --oneline` 核對 commit hash 再往下判斷。
