# Python 編譯快取 / 暫存
__pycache__/
*.pyc
*.pyo

# 程式執行時產生的輸出（在使用者本機才會有）
透析排班/輸出/
__pycache__/
*.pyc
透析排班/webapp/輸出/

# 安全稽查H4（2026-08-10）：這三份是真人姓名/卡號/排班稽核歷史的本機備份，
# 之前一直被提交進這個公開repo，即使檔案內容清空、git歷史裡還是永遠找得到。
# 雲端 Google Sheets 才是主要資料來源，這幾份只是「雲端連不到時」的保底備援，
# 不該再進版控。已用 git rm --cached 拿掉追蹤，這裡確保以後不會又被誤加回去。
# ⚠️ 拿掉追蹤代表雲端斷線時的保底內容會是空的（優雅降級，不會crash，但排班/
# 稽核演算法會找不到歷史、組員名單也是空的），這是刻意的取捨，見接續包第三十二節。
透析排班/webapp/組員名單.csv
透析排班/webapp/排班紀錄.csv
透析排班/webapp/稽核紀錄.csv
