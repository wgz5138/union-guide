# 🔖 透析藥水排班系統 — 接續包（v3.31）

> **給下一個 AI session**：打開這份文件就知道整套系統怎麼運作、現在在哪個版本、有什麼 bug、
> 有什麼鐵律。**不要問使用者已經在這裡寫過的事情。**

---

## ⚠️ AI 接手鐵律（每次開新 session 必讀、必遵守）

### 鐵律一：玉繡排的班表是最高準則
- **「一切以玉繡排的為最高準則」**。玉繡手動排好、存到 Google Sheets 的結果就是最終答案。
- 演算法排出來的是「建議」，玉繡在 Streamlit 點格子微調後才是定案。
- 不要因為演算法說 A，就否定玉繡說 B。**玉繡說的才對。**

### 鐵律二：不得任意增刪名單
- **「不要因為查修程式、排查而任意增刪名單」**——統計數字會完全出錯。
- 不可以為了測試、驗證、除錯而修改 `排班紀錄.csv`、`稽核紀錄.csv`、`組員名單.csv`。
- 只有玉繡指示才能動名單。

### 鐵律三：設計目的
- **「做這程式是要降低玉繡人為的工作量」**，要以玉繡設的規矩為先做演算，玉繡手動修改為次。
- 努力讓演算法自動產出正確結果，減少玉繡要手動改的次數。

### 鐵律四：URL 格式
- 所有 URL 一律用 `[文字](url)` markdown 格式，絕不貼純文字 URL。

### 鐵律五：未查證不得肯定
- 涉及第三方系統（LINE API、Google、醫院 IT）一律先說「需要確認」，查證後才能肯定。

### 鐵律六：接續包必須更新
- 任何 session 新增或改動任何功能，**必須同步更新本檔**。不更新 = 下一個 session 沒上下文。

---

## 一、三大組件

| 組件 | 位置 / 網址 | 狀態 |
|---|---|---|
| Streamlit 手機排班網頁 | https://union-dialysis.streamlit.app/ | `app.py` **v3.31** 上線中 |
| Google 試算表（當資料庫） | ID: `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js` | 分頁：userid / 對照 / 本週名單 / 排班歷史 / 稽核歷史 |
| Apps Script（LINE 小幫手） | `藥水小幫手_完整版.gs` 上線中 | 收 userId、每日提醒、稽核通知、存取歷史 |

---

## 二、Git 分支與部署

- **Repo**：`wgz5138/union-guide`（公開）
- **透析排班開發分支**：`claude/dialysate-scheduling-rl20mk`
- Streamlit Cloud 從 **main 分支** 的 `app.py` 部署（根目錄）
- 推到 main 才生效；開發先在 `claude/dialysate-scheduling-rl20mk` 改，PR 合併後生效
- Worktree 路徑：`/tmp/streamlit-detail`（對應 `claude/dialysate-scheduling-rl20mk`）

---

## 三、Repo 裡的主要檔案

| 檔案 | 版本 | 角色 |
|---|---|---|
| `app.py` | **v3.31** | Streamlit 手機網頁，呼叫 `排班.py`、`稽核.py` |
| `排班.py` | v0.4 基礎，已大幅改版 | 每週印藥水排班核心（見第四節） |
| `稽核.py` | v0.3 | 每月稽核排班 |
| `藥水小幫手_完整版.gs` | v3 | 貼到 Apps Script |
| `組員名單.csv` | — | 15 人卡號+姓名，快速模式用 |
| `排班紀錄.csv` | — | 公平歷史，程式自動維護 ⚠️ 不可手動亂改 |
| `強制可印.csv` | — | 玉繡指定本週放假但仍可印的人（卡號或姓名） |

---

## 四、排班演算法（`排班.py` assign()）

### 基本規則
- **治療日窗口**：週二～下週一（7天）
- **白班（D開頭）**：在 `prev_treat(T)` 那天（T的前一個治療日）印
- **小夜（E開頭）**：在 `prev_treat(prev_treat(T))` 那天（前兩個治療日）印
- **不能印的班別**：大夜(N)、副值、EP1、副夜1/2 等（見 `不可印班別.csv`）
- **放假/空白/大夜 → 一律不能印**（`kind_area()` 回傳 None）

### 三回合邏輯
```
Round 1：同區、一人一週一次
  → 依「治療日由早到晚」順序（T-ascending）逐格處理
  → 確保早的治療日先確保拿到本區候選人
  → 排序優先：強制可印 > AVOID床 > cost > 印次數 > 名單順序

Round 2：跨區支援（Round 1 沒排到的格）
  → 從「組員較多的區」借人
  → 排序優先：強制可印 > 對方區人數多 > AVOID床 > cost

Round 3：還是空 → 某人印第二次
  → 鐵律：有跨區候選人可印第二次時，絕不先動同區候選人印第二次
  → 盡量間隔久；同日一+二也允許
  → 排序：強制可印 > AVOID床 > 跨區(0)>同區(1) > 間隔 > cost
```

### 強制可印（FORCE_PRINT）
- **功能**：玉繡在 `強制可印.csv` 指定某人（放假週但仍可印），系統將其排序提前
- **效果**：只影響排序優先順序，**不改變資格判斷**
- **重點**：放假/大夜/空白班仍然不能印，強制可印 ≠ 強制讓不合格的人印
- 若指定的人剛好有合法班別（小夜/白班在合法區域），他會排在其他人前面

### kind_area()（資格判斷，不可改）
```python
def kind_area(status, card, d, name=""):
    if d is None: return (None,None)
    rec=status.get(card,{}).get(d)
    if not rec: return (None,None)       # 班表空白（放假不在院）→ 不能印
    cat,shift,zone=rec; s=shift.strip()
    if is_excluded_shift(s): return (None,None)  # 大夜/副值等排除班別 → 不能印
    a=zone_area(zone)
    if a=="未知" and zone.strip():
        UNKNOWN_BEDS.setdefault(zone.strip(),set()).add((name,d)); return (None,None)
    if a in PRINT_AREAS:
        if s.startswith("D"): return ("白",a)
        if s.startswith("E"): return ("夜",a)
    return (None,None)
```

---

## 五、版本歷程（v3.26 ～ v3.31）

| 版本 | 日期 | 重點 |
|---|---|---|
| v3.26 | 2026-07 | Round3 跨區優先於同區印第二次 |
| v3.27 | 2026-07 | 強制可印：玉繡在 UI 指定放假但可印的人 |
| v3.28 | 2026-07-16 | 強制可印優先排序 ✅ + 班表空白補漏 ❌（造成印藥水日期算錯） |
| v3.29 | 2026-07-16 | 移除 v3.28 補漏邏輯；kind_area 嘗試讓強制可印跳排除班別 ❌（邏輯錯誤） |
| v3.30 | 2026-07-16 | 還原 kind_area()，強制可印只做排序優先 ✅ |
| **v3.31** | **2026-07-15** | **修正 Round1 排序 bug：改為治療日由早到晚自然順序** ✅ |

### v3.28 ❌ 的錯在哪
- 新增「班表空白時，找當週其他班別推出所在區域」的 fallback
- 造成怡璇（G645）T=7/17 被算成 7/16 印，而不是 T=7/20 的 7/17 印
- 玉繡說：「你怎麼自作主張變成7/16」→ 已還原

### v3.29 ❌ 的錯在哪
- kind_area() 加了 `if is_excluded_shift(s) and not force: return (None,None)`
- 等於讓副值/放假班的人也能印 → 明顯錯誤
- 玉繡說：「你自己看怡璇7/16放假 你覺得能印嗎」→ 已還原

### v3.31 ✅ 修了什麼（顏凰任 bug）
- **Bug**：Round1 原本用 `sorted(slots, key=lambda s: len(same[s]))` 按候選人數少優先
- 造成 T=7/20（1人）比 T=7/18（2人）先跑，顏凰任被 T=7/20 搶走
- T=7/18 只剩已印過的冠秀 → 黃莉婷被迫印兩次
- 玉繡確認：「顏凰任應該是7/16印7/18喔」
- **修法**：移除 sorted()，`for (T,A) in slots:` 直接用自然順序（T-ascending）

---

## 六、設定檔（CSV）

- `組員名單.csv`（卡號, 姓名）— 15 人，快速模式用
- `床號分區.csv`（區域類型, 床號, 盡量避開Y/N）
- `不可印班別.csv`（班別）— 含 N、副值、EP1、副夜1、副夜2 等
- `休診日.csv`（日期, 說明）— 連假/國定假日
- `排班紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `強制可印.csv` — 玉繡本週手動指定（卡號或姓名），每週可清空重寫

---

## 七、雲端串接（Streamlit Secrets）

```toml
APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbz4iRQfQ8dfHf6FJJJc5bUv4uiyrcSKfVPXb621jZs_WFhhCTSpvBYiq7rsMjrPgUj8XA/exec"
WRITE_SECRET = "yaoshui2026"
```

`藥水小幫手_完整版.gs` 裡的 `WRITE_SECRET` 要與上面一致；`LINE_TOKEN` 貼自己的 Channel access token。

---

## 八、每週操作流程

```
上傳班表（.xls）→ 選分頁 → ➡️ 排印藥水
→ 點格子微調（必要時）→ 強制可印 輸入放假但可印的人
→ ✅ 產生定案 → 🚀 送到雲端
→ 試算表「本週名單」更新 → .gs 每晚 LINE 提醒「明天要印藥水的人」
```

⚠️ **「送到雲端」才是真正更新 Google Sheets**。在 Streamlit 跑演算法、看結果 **不影響** Google Sheets，也不影響 LINE 通知。已送出的定案要改，需在 Streamlit 重新產生後再次送出。

---

## 九、已知待處理事項

1. **玉繡要重新捋規則**：本次 session 使用者說「規則等玉繡重新再跟你捋一次」，下次 session 繼續時，等玉繡說完規則再做演算法更新。

2. **v3.31 待合併**：`claude/dialysate-scheduling-rl20mk` 分支的 v3.31 還未合併到 main，Streamlit Cloud 尚未更新。玉繡確認結果正確後需做 PR → 合併。

---

## 十、想改怎麼辦

跟任何 AI 說：「先看 `透析藥水排班_接續包.md`，然後幫我改 ___」即可。

常見改動位置：
- 排班規則 → `排班.py` 的 `assign()` 三回合邏輯
- 資格判斷 → `排班.py` 的 `kind_area()`、`is_excluded_shift()`
- 強制可印 → `排班.py` 的 `FORCE_PRINT` 全域集合、`is_force()`
- 網頁畫面 → `app.py`
- LINE 訊息文字/提醒時間 → `藥水小幫手_完整版.gs`（`sendReminders`）
