# 🔖 透析藥水排班系統 — 接續包（v3.31）

> **給下一個 AI session**：打開這份文件就知道整套系統怎麼運作、現在在哪個版本、有什麼 bug、
> 有什麼鐵律。**不要問使用者已經在這裡寫過的事情。**

---

## ⚠️ AI 接手鐵律（每次開新 session 必讀、必遵守）

### 鐵律一：玉繡排的班表是最高準則
- **「一切以玉繡排的為最高準則」**。玉繡手動排好、存到 Google Sheets 的結果就是最終答案。
- 演算法排出來的是「建議」，玉繡在 Streamlit 點格子微調後才是定案。
- 不要因為演算法說 A，就否定玉繡說 B。**玉繡說的才對。**

### 鐵律二：不得任意增刪名單
- **「不要因為查修程式、排查而任意增刪名單」**——統計數字會完全出錯。
- 不可以為了測試、驗證、除錯而修改 `排班紀錄.csv`、`稽核紀錄.csv`、`組員名單.csv`。
- 只有玉繡指示才能動名單。

### 鐵律三：設計目的
- **「做這程式是要降低玉繡人為的工作量」**，要以玉繡設的規矩為先做演算，玉繡手動修改為次。
- 努力讓演算法自動產出正確結果，減少玉繡要手動改的次數。

### 鐵律四：URL 格式
- 所有 URL 一律用 `[文字](url)` markdown 格式，絕不貼純文字 URL。

### 鐵律五：未查證不得肯定
- 涉及第三方系統（LINE API、Google、醫院 IT）一律先說「需要確認」，查證後才能肯定。

### 鐵律六：接續包必須更新
- 任何 session 新增或改動任何功能，**必須同步更新本檔**。不更新 = 下一個 session 沒上下文。

---

## 一、三大組件

| 組件 | 位置 / 網址 | 狀態 |
|---|---|---|
| Streamlit 手機排班網頁 | https://union-dialysis.streamlit.app/ | `app.py` **v3.31** 上線中 |
| Google 試算表（當資料庫） | ID: `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js` | 分頁：userid / 對照 / 本週名單 / 排班歷史 / 稽核歷史 |
| Apps Script（LINE 小幫手） | `藥水小幫手_完整版.gs` 上線中 | 收 userId、每日提醒、稽核通知、存取歷史 |

---

## 二、Git 分支與部署

- **Repo**：`wgz5138/union-guide`（公開）
- **透析排班開發分支**：`claude/dialysate-scheduling-rl20mk`
- Streamlit Cloud 從 **main 分支** 的 `app.py` 部署（根目錄）
- 推到 main 才生效；開發先在 `claude/dialysate-scheduling-rl20mk` 改，PR 合併後生效
- Worktree 路徑：`/tmp/streamlit-detail`（對應 `claude/dialysate-scheduling-rl20mk`）

---

## 三、Repo 裡的主要檔案

| 檔案 | 版本 | 角色 |
|---|---|---|
| `app.py` | **v3.31** | Streamlit 手機網頁，呼叫 `排班.py`、`稽核.py` |
| `排班.py` | v0.4 基礎，已大幅改版 | 每週印藥水排班核心（見第四節） |
| `稽核.py` | v0.3 | 每月稽核排班 |
| `藥水小幫手_完整版.gs` | v3 | 貼到 Apps Script |
| `組員名單.csv` | — | 15 人卡號+姓名，快速模式用 |
| `排班紀錄.csv` | — | 公平歷史，程式自動維護 ⚠️ 不可手動亂改 |
| `強制可印.csv` | — | 玉繡指定本週放假但仍可印的人（卡號或姓名） |

---

## 四、每週印藥水排班規則（`排班.py`）

### 班表 Excel 格式
- 表頭列：`卡號 | 姓名 | ... | 類別 | 班別 | 責任區域 | （下一天）類別 | 班別 | 責任區域 | ...`
- 程式找「卡號」「姓名」表頭定位；往上幾列找日期
- 責任區域要和 `床號分區.csv` **一字不差**（例如 `1-3,21` / `25-28`）

### 印藥水日期規則
| 班別 | 印的日期 |
|---|---|
| 白班（D 開頭） | `prev_treat(T)`：T 的前一個治療日 |
| 小夜（E 開頭） | `prev_treat(prev_treat(T))`：T 的前兩個治療日 |
- **prev_treat** 自動跳週日 + 休診日（過年/連假填 `休診日.csv`）

### 不能印的班別（`不可印班別.csv` 預設）
```
N、11-7、2-10、3'-12、1'-10、12-8、EP1
副值、收水、對帳、文書、查帳、W103
1線、2線、3線、4線、8-5'、8-4'、1-9
```
- 班表空白（放假不在院）→ 也不能印
- **is_excluded_shift()** 做全字或含字串比對（≥3 字元的 token 用 `in` 比對）

### 資格判斷 kind_area()（**此函式不可更動**）
```python
def kind_area(status, card, d, name=""):
    if d is None: return (None,None)
    rec=status.get(card,{}).get(d)
    if not rec: return (None,None)         # 班表空白（放假）→ 不能印
    cat,shift,zone=rec; s=shift.strip()
    if is_excluded_shift(s): return (None,None)   # 排除班別 → 不能印
    a=zone_area(zone)
    if a=="未知" and zone.strip():
        UNKNOWN_BEDS...add((name,d)); return (None,None)   # 未知床號 → 警告且不能印
    if a in PRINT_AREAS:
        if s.startswith("D"): return ("白",a)
        if s.startswith("E"): return ("夜",a)
    return (None,None)
```

### 床號分區規則
- `床號分區.csv` 欄位：**區域類型, 床號, 盡量避開Y/N**
- 區域類型：`一區` / `二區`（= PRINT_AREAS，可列印）；`排除`（病房/ICU）；`勤務`（非床邊）
- 盡量避開 Y 的床號（22-23, 29-30, 60-63）：排序時排後面，有其他人才輪到他們

### 公平輪序（cost）
- `cost = 印次數 - 休息次數`；負值越大（休越多）越優先排印
- **整週沒印到的人 = 本週「休息」，一律計入公平輪序**，包括病房/ICU/大夜/整週休假
- 公平歷史從 Google Sheets「排班歷史」拉回，`app.py` 送出時同步寫回
- 本週的歷史以 `skip_week=分頁名稱` 排除，避免重算

### 三回合核心邏輯

**Round 1：同區、一人一週一次**
```
治療日由早到晚（T-ascending）順序逐格
同區候選人 → 排序鍵：(is_force, AVOID床, cost, 本週已印天數, 名單順序)
搶到後標記 member_days[c]，同一個人不能再被同一週其他格搶走
```

**Round 2：跨區支援**（Round 1 沒排到的格才跑）
```
從其他區借人（仍一人一週一次）
排序鍵：(is_force, -對方區同區候選人數(越多越好借), AVOID床, cost, 本週已印天數, 名單順序)
警告：「※ X區 沒有本區的人 → 從『Y區』借『名字』過來印」
```

**Round 3：還是空 → 印第二次**（Round 1+2 都排不出才跑）
```
每人最多印 2 次（cap）
鐵律：有跨區候選人可印第二次 → 絕不先動同區候選人印第二次
盡量間隔久（gap 越大越優先）；同日一+二也允許
排序鍵：(is_force, AVOID床, 跨區=0>同區=1, -gap, cost, 名單順序)
警告：「※ X 人手不足 → 安排『名字』跨區/同區印第二次(替代方案)」
```

**排不出**：標 ❌，輸出「❌ 完全排不出人！請手動處理」

### 輸出標記
| 標記 | 意義 |
|---|---|
| `(X/Y印)` | 幾月幾日印藥水 |
| `🔺` | 跨區支援 |
| `雙印` | 本週印第二次 |

### 強制可印（FORCE_PRINT）
- 玉繡在 `強制可印.csv` 指定某人（放假週但剛好排到合法班別仍可印）
- **效果**：排序優先排他，`is_force` → sort key 0（其他人 1）
- **不改資格**：放假/大夜/空白班依然不能印
- 若指定的人本週完全沒有合法班別，強制可印無效（不會硬塞進去）

---

## 五、每月稽核排班規則（`稽核.py`）

### 稽核架構
每月排 **12 個稽核位置**：

| 維度 | 選項 |
|---|---|
| 區域 | 一區、二區（2個） |
| 組別 | W135（週一三五）、W246（週二四六）（2個） |
| 班次 | 第一班、第二班、第三班（3個） |

2 × 2 × 3 = **12 個 slot**

### 班次 vs 班型對應規則
| 班次 | 誰能排 |
|---|---|
| 第一班 | 白班 |
| 第二班 | 白班（優先）；若白班人數 < 夜班人數，小夜也可補 |
| 第三班 | 小夜 |

### 班型自動判斷（第一週小班）
- 從**當月第一週班表**看每人白班（D）/ 小夜（E）天數
- 白班天數 ≥ 小夜天數 → 判為「白班」
- 白 < 夜 → 判為「夜班」
- 完全排除班（大夜等）→ None（無法稽核）
- **玉繡可在 Stage 1 手動覆蓋班型**（網頁點選），優先於自動判斷
- 班型覆蓋寫入 `班型覆蓋.csv`（卡號, 姓名, 班型）

### 區域判斷
- 同樣從**第一週班表**，看責任區域出現最多的是一區還是二區
- 若第一週找不到，改用整月統計

### 三階段排法（稽核 assign()）
```
Round 1：同區、各班次挑最公平的人（cost = 稽核次 - 休息次）
  → 先跑「同區候選人最少的 slot」（讓稀缺格優先被保障）
Round 2：跨區補位（同區排不出才跑）
  → 警告：「※ X區 第N班 本區排不出 → 跨區借『名字』」
排不出：標 ❌，警告「請人工處理」
每月沒稽核到的人 = 本月「休息」，計入公平輪序
```

### 快速模式（免上傳 Excel）
- 網頁玉繡直接點選每人 白班/小夜 與 一區/二區
- 寫入 `快速名冊.csv`（卡號, 姓名, 班型, 區）
- `python 稽核.py --quick 2026-06`
- 輸出格式與 Excel 模式**完全相同**

---

## 五、版本歷程（v3.26 ～ v3.31）

| 版本 | 日期 | 重點 |
|---|---|---|
| v3.26 | 2026-07 | Round3 跨區優先於同區印第二次 |
| v3.27 | 2026-07 | 強制可印：玉繡在 UI 指定放假但可印的人 |
| v3.28 | 2026-07-16 | 強制可印優先排序 ✅ + 班表空白補漏 ❌（造成印藥水日期算錯） |
| v3.29 | 2026-07-16 | 移除 v3.28 補漏邏輯；kind_area 嘗試讓強制可印跳排除班別 ❌（邏輯錯誤） |
| v3.30 | 2026-07-16 | 還原 kind_area()，強制可印只做排序優先 ✅ |
| **v3.31** | **2026-07-15** | **修正 Round1 排序 bug：改為治療日由早到晚自然順序** ✅ |

### v3.28 ❌ 的錯在哪
- 新增「班表空白時，找當週其他班別推出所在區域」的 fallback
- 造成怡璇（G645）T=7/17 被算成 7/16 印，而不是 T=7/20 的 7/17 印
- 玉繡說：「你怎麼自作主張變成7/16」→ 已還原

### v3.29 ❌ 的錯在哪
- kind_area() 加了 `if is_excluded_shift(s) and not force: return (None,None)`
- 等於讓副值/放假班的人也能印 → 明顯錯誤
- 玉繡說：「你自己看怡璇7/16放假 你覺得能印嗎」→ 已還原

### v3.31 ✅ 修了什麼（顏凰任 bug）
- **Bug**：Round1 原本用 `sorted(slots, key=lambda s: len(same[s]))` 按候選人數少優先
- 造成 T=7/20（1人）比 T=7/18（2人）先跑，顏凰任被 T=7/20 搶走
- T=7/18 只剩已印過的冠秀 → 黃莉婷被迫印兩次
- 玉繡確認：「顏凰任應該是7/16印7/18喔」
- **修法**：移除 sorted()，`for (T,A) in slots:` 直接用自然順序（T-ascending）

---

## 六、設定檔（CSV）

- `組員名單.csv`（卡號, 姓名）— 15 人，快速模式用
- `床號分區.csv`（區域類型, 床號, 盡量避開Y/N）
- `不可印班別.csv`（班別）— 含 N、副值、EP1 等（見第四節完整列表）
- `休診日.csv`（日期, 說明）— 連假/國定假日
- `排班紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `稽核紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `強制可印.csv` — 玉繡本週手動指定（卡號或姓名），每週可清空重寫

---

## 七、歷史統計資料位置（非常重要，不可斷層）

### 資料在哪裡

| 資料 | 主要位置 | 備份位置 |
|---|---|---|
| 印藥水歷史 | Google Sheets「排班歷史」分頁 | `排班紀錄.csv`（branch: `claude/dialysate-scheduling-rl20mk`） |
| 稽核歷史 | Google Sheets「稽核歷史」分頁 | `稽核紀錄.csv`（同上） |

**Google Sheets 才是主要資料來源**。`app.py` 每次開啟時從 GSheets 拉取歷史；送出定案時寫回。CSV 是本機備份。

### 資料量（截至 2026-07-15）
- `排班紀錄.csv`：**374 筆**，從 2025-01-12（`0112`）到 2026-07-13~19（`0713~0719`）
- `稽核紀錄.csv`：**152 筆**，從 114-09（2025-09月）到 115-07（2026-07月）

### CSV 格式
**排班紀錄.csv**（印藥水）：
```
週次, 卡號, 姓名, 狀態, 治療日
0713~0719, 5379, 巫佳容, 印, 週四7/16
0713~0719, G645, 黃怡璇, 休,
```
- 狀態：`印`（本週排到）/ `休`（本週沒排到，含病房/大夜/休假）
- 一整週的人全部都有記錄（印+休合計 = 全組員）

**稽核紀錄.csv**（稽核）：
```
月份, 卡號, 姓名, 狀態, 位置
115-07, 4243, 黃莉婷, 稽核, 二區/週二四六/第二班
115-07, G461, 陳葒萍, 休,
```
- 狀態：`稽核`（本月排到）/ `休`（本月沒排到）

### 新 session 存取方式
新對話可以用以下方式讀取歷史數據：
1. **直接讀 CSV**：checkout `claude/dialysate-scheduling-rl20mk` 分支後，在 `/透析排班/webapp/` 下可以找到兩個 CSV
2. **從 GSheets 看**：Google Sheets ID `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js`，分頁「排班歷史」和「稽核歷史」

---

## 八、雲端串接（Streamlit Secrets）

```toml
APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbz4iRQfQ8dfHf6FJJJc5bUv4uiyrcSKfVPXb621jZs_WFhhCTSpvBYiq7rsMjrPgUj8XA/exec"
WRITE_SECRET = "yaoshui2026"
```

`藥水小幫手_完整版.gs` 裡的 `WRITE_SECRET` 要與上面一致；`LINE_TOKEN` 貼自己的 Channel access token。

---

## 九、每週操作流程

```
上傳班表（.xls）→ 選分頁 → ➡️ 排印藥水
→ 點格子微調（必要時）→ 強制可印 輸入放假但可印的人
→ ✅ 產生定案 → 🚀 送到雲端
→ 試算表「本週名單」更新 → .gs 每晚 LINE 提醒「明天要印藥水的人」
```

⚠️ **「送到雲端」才是真正更新 Google Sheets**。在 Streamlit 跑演算法、看結果 **不影響** Google Sheets，也不影響 LINE 通知。已送出的定案要改，需在 Streamlit 重新產生後再次送出。

---

## 十、已知待處理事項

1. **玉繡要重新捋規則**：本次 session 使用者說「規則等玉繡重新再跟你捋一次」，下次 session 繼續時，等玉繡說完規則再做演算法更新。

2. **v3.31 待合併**：`claude/dialysate-scheduling-rl20mk` 分支的 v3.31 還未合併到 main，Streamlit Cloud 尚未更新。玉繡確認結果正確後需做 PR → 合併。

---

## 十一、想改怎麼辦

跟任何 AI 說：「先看 `透析藥水排班_接續包.md`，然後幫我改 ___」即可。

常見改動位置：
- 排班規則 → `排班.py` 的 `assign()` 三回合邏輯
- 資格判斷 → `排班.py` 的 `kind_area()`、`is_excluded_shift()`
- 強制可印 → `排班.py` 的 `FORCE_PRINT` 全域集合、`is_force()`
- 網頁畫面 → `app.py`
- LINE 訊息文字/提醒時間 → `藥水小幫手_完整版.gs`（`sendReminders`）
