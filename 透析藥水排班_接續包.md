# 🔖 透析藥水排班系統 — 接續包（現況：v3.30）

> **給下一個 AI session**：打開這份文件就知道整套系統怎麼運作、現在在哪個版本、有什麼 bug、
> 有什麼鐵律。**不要問使用者已經在這裡寫過的事情。**
>
> ⚠️ **這份文件在 2026-07-15 大幅重寫過一次**，原因：上一版（2026-07-15 13:26～13:37 寫的）
> 把「剛做完、還沒驗證會不會有副作用」的 v3.31 直接寫成✅定案，結果 90 分鐘後（15:05）
> 那個修法就因為造成雙印回歸被撤銷了，文件卻沒跟著更新。同時上一版對「Streamlit 到底
> 部署哪個檔案」的描述整段是錯的。這次重寫已經逐項跟程式碼、跟玉繡的 Streamlit Cloud
> 後台設定核對過，見第十一節「文件曾經錯在哪」。

---

## ⚠️ AI 接手鐵律（每次開新 session 必讀、必遵守）

### 鐵律一：玉繡排的班表是最高準則
- **「一切以玉繡排的為最高準則」**。玉繡手動排好、存到 Google Sheets 的結果就是最終答案。
- 演算法排出來的是「建議」，玉繡在 Streamlit 點格子微調後才是定案。
- 不要因為演算法說 A，就否定玉繡說 B。**玉繡說的才對。**

### 鐵律二：不得任意增刪名單
- **「不要因為查修程式、排查而任意增刪名單」**——統計數字會完全出錯。
- 不可以為了測試、驗證、除錯而修改 `排班紀錄.csv`、`稽核紀錄.csv`、`組員名單.csv`。
- 只有玉繡指示才能動名單。

### 鐵律三：設計目的
- **「做這程式是要降低玉繡人為的工作量」**，要以玉繡設的規矩為先做演算，玉繡手動修改為次。
- 努力讓演算法自動產出正確結果，減少玉繡要手動改的次數。

### 鐵律四：URL 格式
- 所有 URL 一律用 `[文字](url)` markdown 格式，絕不貼純文字 URL。

### 鐵律五：未查證不得肯定
- 涉及第三方系統（LINE API、Google、Streamlit Cloud 後台、醫院 IT）一律先說「需要確認」，
  查證後才能肯定。**Streamlit Cloud 的部署設定尤其容易搞錯，務必跟玉繡核對後台設定，
  不要憑接續包舊文字或猜測。**

### 鐵律六：接續包必須更新
- 任何 session 新增或改動任何功能，**必須同步更新本檔**。不更新 = 下一個 session 沒上下文。
- **改完程式碼後，如果後來又反悔/撤銷，接續包也要跟著改回去**——不能只更新「做了什麼」，
  忘記更新「後來被撤銷了」。上一版就是栽在這裡（見第十一節）。

---

## 一、三大組件

| 組件 | 位置 / 網址 | 狀態 |
|---|---|---|
| Streamlit 手機排班網頁 | https://union-dialysis.streamlit.app/ | `透析排班/webapp/app.py`（分支 `claude/dialysate-scheduling-rl20mk`）**v3.30** 上線中 |
| Google 試算表（當資料庫） | ID: `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js` | 分頁：userid / 對照 / 本週名單 / 排班歷史 / 稽核歷史 / 排班草稿 |
| Apps Script（LINE 小幫手） | `透析排班/cloud/藥水小幫手_完整版.gs` 上線中 | 收 userId、每日提醒、稽核通知、存取歷史 |

---

## 二、Git 分支與部署（⚠️ 之前寫錯過，這版已跟玉繡的 Streamlit Cloud 後台核對）

- **Repo**：`wgz5138/union-guide`（公開）
- **真正被 Streamlit Cloud 部署的檔案**：`claude/dialysate-scheduling-rl20mk` 分支的
  **`透析排班/webapp/app.py`**（已直接跟玉繡的 Streamlit Cloud「App settings → Main file path」
  核對過，不是猜的）
- **推到 `claude/dialysate-scheduling-rl20mk` 分支就直接生效，沒有 PR / 合併到 main 這一關**。
  main 分支跟這個 Streamlit app 的部署**完全無關**。
- Worktree 路徑：`/tmp/streamlit-detail`（`git worktree add /tmp/streamlit-detail
  claude/dialysate-scheduling-rl20mk`）。**注意檔案在 worktree 底下的 `透析排班/webapp/`
  子目錄，不是 worktree 根目錄。**

### ⚠️ 陷阱：main 根目錄有一份「沒人在用」的舊複製品
main 分支的**根目錄**也放著一份同名的 `排班.py`、`app.py`、`稽核.py`、`組員名單.csv`。
這份是舊複製品，**不是實際部署的程式**，特徵：
- 沒有 `FORCE_PRINT`／強制可印功能（真正部署的那份有）
- 沒有 `requirements.txt`（Streamlit 部署一定要有，這點就能判斷這份不是入口）
- `組員名單.csv` 姓名還是錯字「余伶緣」，真正部署的那份已經修正為「余怜緣」

**曾經有 session 誤改到這份根目錄複製品，浪費了一輪查證才發現改錯地方。**
下次要改排班/稽核邏輯，**第一件事先確認自己在 `claude/dialysate-scheduling-rl20mk` 分支的
`透析排班/webapp/` 底下**，不是 main 根目錄。（這份舊複製品要不要直接砍掉，待玉繡/小巫決定，
目前先保留但已加這段警示。）

---

## 三、Repo 裡的主要檔案（路徑：`透析排班/webapp/`，分支 `claude/dialysate-scheduling-rl20mk`）

| 檔案 | 版本 | 角色 |
|---|---|---|
| `透析排班/webapp/app.py` | **v3.30** | Streamlit 手機網頁，呼叫 `排班.py`、`稽核.py` |
| `透析排班/webapp/排班.py` | v0.4 基礎，已大幅改版 | 每週印藥水排班核心（見第四節） |
| `透析排班/webapp/稽核.py` | v0.3 | 每月稽核排班 |
| `透析排班/cloud/藥水小幫手_完整版.gs` | v3 | 貼到 Apps Script |
| `透析排班/webapp/組員名單.csv` | — | 15 人卡號+姓名，快速模式用 |
| `透析排班/webapp/排班紀錄.csv` | — | 公平歷史，程式自動維護 ⚠️ 不可手動亂改 |
| `透析排班/webapp/稽核紀錄.csv` | — | 稽核歷史，程式自動維護 ⚠️ 不可手動亂改 |
| `透析排班/webapp/強制可印.csv` | — | 玉繡指定本週放假但仍可印的人（卡號或姓名） |

---

## 四、每週印藥水排班規則（`排班.py`）

### 班表 Excel 格式
- 表頭列：`卡號 | 姓名 | ... | 類別 | 班別 | 責任區域 | （下一天）類別 | 班別 | 責任區域 | ...`
- 程式找「卡號」「姓名」表頭定位；往上幾列找日期
- 責任區域要和 `床號分區.csv` **一字不差**（例如 `1-3,21` / `25-28`）

### 印藥水日期規則
| 班別 | 印的日期 |
|---|---|
| 白班（D 開頭） | `prev_treat(T)`：T 的前一個治療日 |
| 小夜（E 開頭） | `prev_treat(prev_treat(T))`：T 的前兩個治療日 |
- **prev_treat** 自動跳週日 + 休診日（過年/連假填 `休診日.csv`）

### 不能印的班別（`不可印班別.csv` 預設）
```
N、11-7、2-10、3'-12、1'-10、12-8、EP1
副值、收水、對帳、文書、查帳、W103
1線、2線、3線、4線、8-5'、8-4'、1-9
```
- 班表空白（放假不在院）→ 也不能印
- **is_excluded_shift()** 做全字或含字串比對（≥3 字元的 token 用 `in` 比對）

### 資格判斷 kind_area()（**此函式不可更動，已於 2026-07-15 跟現行程式碼逐行核對**）
```python
def kind_area(status, card, d, name=""):
    if d is None: return (None,None)
    rec=status.get(card,{}).get(d)
    if not rec: return (None,None)          # 班表空白（放假不在院）→ 不能印
    cat,shift,zone=rec; s=shift.strip()
    if is_excluded_shift(s): return (None,None)   # 大夜/副值等排除班別 → 不能印
    a=zone_area(zone)
    if a=="未知" and zone.strip():
        UNKNOWN_BEDS.setdefault(zone.strip(),set()).add((name,d)); return (None,None)
    if a in PRINT_AREAS:
        if s.startswith("D"): return ("白",a)
        if s.startswith("E"): return ("夜",a)
    return (None,None)
```

### 床號分區規則
- `床號分區.csv` 欄位：**區域類型, 床號, 盡量避開Y/N**
- 區域類型：`一區` / `二區`（= PRINT_AREAS，可列印）；`排除`（病房/ICU）；`勤務`（非床邊）
- 盡量避開 Y 的床號（22-23, 29-30, 60-63）：排序時排後面，有其他人才輪到他們

### 公平輪序（cost）
- `cost = 印次數 - 休息次數`；負值越大（休越多）越優先排印
- **整週沒印到的人 = 本週「休息」，一律計入公平輪序**，包括病房/ICU/大夜/整週休假
- 公平歷史從 Google Sheets「排班歷史」拉回，`app.py` 送出時同步寫回
- 本週的歷史以 `skip_week=分頁名稱` 排除，避免重算

### 三回合核心邏輯（現況：v3.30，**Round 1 目前仍有已知 bug，見下方**）

**Round 1：同區、一人一週一次**
```python
for (T,A) in sorted(slots, key=lambda s: len(same[s])):   # ⚠️ 現況：候選人數少的格子優先跑
    ...
```
⚠️ **已知問題（尚未修復，待玉繡重新捋規則）**：這個排序會讓「候選人少」的治療日搶先處理，
可能把本來該給「候選人少但日期較早」那格的人搶到別的日子去，造成別人被迫印第二次。
2026-07-15 曾經試過改成「治療日由早到晚」的自然順序（v3.31），**確實修好了原本的錯位**，
但同時造成另外兩人（潘冠秀、余怜緣）雙印，比修之前的單一雙印還糟，已撤銷回目前這版。
**下次要動這裡之前，必須先跟玉繡把規則捋清楚（哪些情況允許雙印、優先序到底怎麼定），
不要只解決眼前這一週的錯位就直接上。**

**Round 2：跨區支援**（Round 1 沒排到的格才跑）
```
從其他區借人（仍一人一週一次）
排序鍵：(is_force, -對方區同區候選人數(越多越好借), AVOID床, cost, 本週已印天數, 名單順序)
警告：「※ X區 沒有本區的人 → 從『Y區』借『名字』過來印」
```

**Round 3：還是空 → 印第二次**（Round 1+2 都排不出才跑）
```
每人最多印 2 次（cap）
鐵律：有跨區候選人可印第二次 → 絕不先動同區候選人印第二次
盡量間隔久（gap 越大越優先）；同日一+二也允許
排序鍵：(is_force, AVOID床, 跨區=0>同區=1, -gap, cost, 名單順序)
警告：「※ X 人手不足 → 安排『名字』跨區/同區印第二次(替代方案)」
```

**排不出**：標 ❌，輸出「❌ 完全排不出人！請手動處理」

### 輸出標記
| 標記 | 意義 |
|---|---|
| `(X/Y印)` | 幾月幾日印藥水 |
| `🔺` | 跨區支援 |
| `雙印` | 本週印第二次 |

### 強制可印（FORCE_PRINT，已跟現行程式碼核對，描述正確）
- 玉繡在 `強制可印.csv` 指定某人（放假週但剛好排到合法班別仍可印）
- **效果**：排序優先排他，`is_force` → sort key 0（其他人 1）
- **不改資格**：放假/大夜/空白班依然不能印
- 若指定的人本週完全沒有合法班別，強制可印無效（不會硬塞進去）

---

## 五、每月稽核排班規則（`稽核.py`）（已跟現行程式碼核對，架構描述正確）

### 稽核架構
每月排 **12 個稽核位置**：

| 維度 | 選項 |
|---|---|
| 區域 | 一區、二區（2個） |
| 組別 | W135（週一三五）、W246（週二四六）（2個） |
| 班次 | 第一班、第二班、第三班（3個） |

2 × 2 × 3 = **12 個 slot**

### 班次 vs 班型對應規則
| 班次 | 誰能排 |
|---|---|
| 第一班 | 白班 |
| 第二班 | 白班（優先）；若白班人數 < 夜班人數，小夜也可補 |
| 第三班 | 小夜 |

### 班型自動判斷（第一週小班）
- 從**當月第一週班表**看每人白班（D）/ 小夜（E）天數
- 白班天數 ≥ 小夜天數 → 判為「白班」
- 白 < 夜 → 判為「夜班」
- 完全排除班（大夜等）→ None（無法稽核）
- **玉繡可在 Stage 1 手動覆蓋班型**（網頁點選），優先於自動判斷
- 班型覆蓋寫入 `班型覆蓋.csv`（卡號, 姓名, 班型）

### 區域判斷
- 同樣從**第一週班表**，看責任區域出現最多的是一區還是二區
- 若第一週找不到，改用整月統計

### 三階段排法（稽核 assign()）
```
Round 1：同區、各班次挑最公平的人（cost = 稽核次 - 休息次）
  → 先跑「同區候選人最少的 slot」（讓稀缺格優先被保障）
Round 2：跨區補位（同區排不出才跑）
  → 警告：「※ X區 第N班 本區排不出 → 跨區借『名字』」
排不出：標 ❌，警告「請人工處理」
每月沒稽核到的人 = 本月「休息」，計入公平輪序
```

### 快速模式（免上傳 Excel）
- 網頁玉繡直接點選每人 白班/小夜 與 一區/二區
- 寫入 `快速名冊.csv`（卡號, 姓名, 班型, 區）
- `python 稽核.py --quick 2026-06`
- 輸出格式與 Excel 模式**完全相同**

---

## 六、設定檔（CSV，全部在 `透析排班/webapp/` 底下）

- `組員名單.csv`（卡號, 姓名）— 15 人，快速模式用
- `床號分區.csv`（區域類型, 床號, 盡量避開Y/N）
- `不可印班別.csv`（班別）— 含 N、副值、EP1 等（見第四節完整列表）
- `休診日.csv`（日期, 說明）— 連假/國定假日
- `排班紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `稽核紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `強制可印.csv` — 玉繡本週手動指定（卡號或姓名），每週可清空重寫

---

## 七、歷史統計資料位置（2026-07-15 逐筆重新核對過，修正了一個年份錯誤）

### 資料在哪裡

| 資料 | 主要位置 | 備份位置 |
|---|---|---|
| 印藥水歷史 | Google Sheets「排班歷史」分頁 | `透析排班/webapp/排班紀錄.csv`（branch: `claude/dialysate-scheduling-rl20mk`） |
| 稽核歷史 | Google Sheets「稽核歷史」分頁 | `透析排班/webapp/稽核紀錄.csv`（同上） |

**Google Sheets 才是主要資料來源**。`app.py` 每次開啟時從 GSheets 拉取歷史；送出定案時寫回。CSV 是本機備份。

### 資料量（截至 2026-07-15，實際數過）
- `排班紀錄.csv`：**374 筆**（375 行含表頭），週次從 `0112` 到 `0713~0719`，**逐週推算實際是
  2026-01-12 ～ 2026-07-19**（⚠️ 上一版接續包寫「2025-01-12」是錯的，差一年——26週的資料量
  塞不進「2025年初到2026年中」這麼長的區間，逐週往回推算落點在 2026 年初，已修正）
- `排班紀錄.csv` **有一週資料缺口**：`0706~0712` 這週完全沒有紀錄（`0629` 之後直接跳到
  `0713~0719`），原因未查（可能那週沒送出定案，或漏記）。上一版接續包完全沒提到這個缺口，
  下次做統計/公平輪序分析時要注意這一週是斷的，不是印藥水次數真的是 0。
- `稽核紀錄.csv`：**152 筆**（153行含表頭），從 `114-09`（民國114年9月＝2025-09）到
  `115-07`（民國115年7月＝2026-07），逐月核對無缺口

### CSV 格式
**排班紀錄.csv**（印藥水）：
```
週次, 卡號, 姓名, 狀態, 治療日
0713~0719, 5379, 巫佳容, 印, 週四7/16
0713~0719, G645, 黃怡璇, 休,
```
- 狀態：`印`（本週排到）/ `休`（本週沒排到，含病房/大夜/休假）
- 一整週的人全部都有記錄（印+休合計 = 全組員）
- **「治療日」欄記的是 T（治療日），不是印藥水日**，兩者不是同一天，讀資料時不要搞混

**稽核紀錄.csv**（稽核）：
```
月份, 卡號, 姓名, 狀態, 位置
115-07, 4243, 黃莉婷, 稽核, 二區/週二四六/第二班
115-07, G461, 陳葒萍, 休,
```
- 狀態：`稽核`（本月排到）/ `休`（本月沒排到）

### 新 session 存取方式
新對話可以用以下方式讀取歷史數據：
1. **直接讀 CSV**：`git worktree add /tmp/streamlit-detail claude/dialysate-scheduling-rl20mk`，
   兩個 CSV 在 `/tmp/streamlit-detail/透析排班/webapp/` 底下（**不是 worktree 根目錄**）
2. **從 GSheets 看**：Google Sheets ID `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js`，分頁「排班歷史」和「稽核歷史」

---

## 八、雲端串接（Streamlit Secrets）

```toml
APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbz4iRQfQ8dfHf6FJJJc5bUv4uiyrcSKfVPXb621jZs_WFhhCTSpvBYiq7rsMjrPgUj8XA/exec"
WRITE_SECRET = "yaoshui2026"
```

`藥水小幫手_完整版.gs` 裡的 `WRITE_SECRET` 要與上面一致；`LINE_TOKEN` 貼自己的 Channel access token。

`sendReminders()` 邏輯很單純：直接比對 Google Sheets「本週名單」分頁的「印日期」欄是不是等於
明天的日期，是就發 LINE。**沒有自己的日期運算，不會有時區/off-by-one 這類 bug**——如果 LINE
提醒的日期不對，问题一定出在「本週名單」分頁被寫入了錯的資料，不是這支 .gs 本身算錯。

---

## 九、每週操作流程

```
上傳班表（.xls）→ 選分頁 → ➡️ 排印藥水
→ 點格子微調（必要時）→ 強制可印 輸入放假但可印的人
→ ✅ 產生定案 → 🚀 送到雲端
→ 試算表「本週名單」更新 → .gs 每晚 LINE 提醒「明天要印藥水的人」
```

⚠️ **「送到雲端」才是真正更新 Google Sheets**。在 Streamlit 跑演算法、看結果 **不影響** Google Sheets，也不影響 LINE 通知。已送出的定案要改，需在 Streamlit 重新產生後再次送出。

### ⚠️ 重跑本週排班前，先「🗑 清除草稿」——不然舊資料會偷偷蓋回來
`app.py`「產生定案」有一段「自動套回上次定案的修改」邏輯：只要 sheet0（週次分頁名）跟上次
存的草稿一樣，就會自動把上次存過的每一格覆蓋回新算出來的結果，目的是保留玉繡手動改過的地方
（例如「顏凰任→張雅雯」）不用每次重改。**但這個邏輯分不出「這是玉繡手動改的」還是「這是上一次
程式還有 bug 時算出來的錯結果」**——連程式自己的註解都寫「同日多人時仍可能偏差」。

**2026-07-15 已經證實會出事一次**：程式今天中午一度算錯（v3.31 那次），錯的結果被存成草稿；
後來雖然程式碼改回正確版本，但重開 app 時「自動套回上次定案」把舊的錯結果蓋回來，導致玉繡
送到雲端、LINE 也真的通知了錯的日期。

**所以：只要今天程式碼被改過（不管是修 bug 還是玉繡自己調規則），重新排這一週之前務必先按
「🗑 清除草稿」，重新「產生定案」後仔細核對格子內容，確認沒問題再「送到雲端」。**

---

## 十、已知待處理事項

1. **玉繡要重新捋 Round 1 排序規則**：目前的「候選人少優先」排序有已知 bug（見第四節），
   直接改成「治療日自然順序」雖然修好錯位，卻會造成別人雙印（潘冠秀、余怜緣曾經因此雙印）。
   需要玉繡講清楚規則後才能重新設計排序邏輯，不要只顧眼前這週。

2. **main 根目錄的舊複製品要不要清掉**：待玉繡/小巫決定是否直接刪除，目前先在文件加警示（見
   第二節）避免下次又改錯地方。

3. **`0706~0712` 這週排班紀錄缺失**：原因未查，暫時當作已知資料缺口記錄，不要因為要「補齊」
   就自己捏造那週的資料（違反鐵律二）。

---

## 十一、文件曾經錯在哪（2026-07-15 稽核記錄，避免同樣的錯再發生一次）

上一版接續包（2026-07-15 13:26 與 13:37 兩次 commit）出的問題，具體如下：

1. **把「剛做完、還沒驗證」的修法寫成✅定案**：v3.31（12:59 做的）在 13:26/13:37 就被寫成
   最終正確版本，但 15:05 就因為造成雙印回歸被撤銷了，文件沒有跟著改回來。**教訓：剛做完的
   修法，至少要等玉繡實際跑過、確認沒有新的副作用，才能在文件裡寫✅。**

2. **部署架構整段憑印象/猜測寫錯**：舊版寫「Streamlit 從 main 分支根目錄的 app.py 部署，
   推到 main 才生效，開發先在分支改、PR 合併後生效」，但實際上 Streamlit Cloud 的 Main file
   path 設定的是 `claude/dialysate-scheduling-rl20mk` 分支的 `透析排班/webapp/app.py`，推那個
   分支就直接生效，**根本沒有 PR 合併這一步**。**教訓：部署方式這種事屬於鐵律五「第三方系統
   未查證不得肯定」，要嘛去看 Streamlit Cloud 後台實際設定，要嘛直接問玉繡/小巫，不能憑舊文件
   或猜測寫。**

3. **檔案清單沒寫路徑，害人改錯檔案**：舊版檔案清單只寫檔名沒寫路徑，導致真的有 session 跑去
   改 main 根目錄那份沒人用的舊複製品。**教訓：檔案清單一定要連分支、路徑一起寫死。**

4. **統計數據年份寫錯**：`排班紀錄.csv` 374 筆的起始日期舊版寫「2025-01-12」，實際逐週推算是
   **2026-01-12**（差一年）。**教訓：統計數據要嘛實際數過/推算過，要嘛註明「未核對」，不能照抄
   一個沒驗證過的日期。**

5. **`0706~0712` 資料缺口完全沒提到**：這不是「錯」，是「漏」——舊版統計數據只講了「有多少筆、
   從哪到哪」，沒有實際逐週檢查中間有沒有斷。

---

## 十二、想改怎麼辦

跟任何 AI 說：「先看 `透析藥水排班_接續包.md`，然後幫我改 ___」即可。

**但務必提醒 AI：真正要改的程式在 `claude/dialysate-scheduling-rl20mk` 分支的
`透析排班/webapp/` 底下，不是 main 根目錄同名檔案（見第二節陷阱說明）。**

常見改動位置：
- 排班規則 → `透析排班/webapp/排班.py` 的 `assign()` 三回合邏輯
- 資格判斷 → `透析排班/webapp/排班.py` 的 `kind_area()`、`is_excluded_shift()`
- 強制可印 → `透析排班/webapp/排班.py` 的 `FORCE_PRINT` 全域集合、`is_force()`
- 網頁畫面 → `透析排班/webapp/app.py`
- LINE 訊息文字/提醒時間 → `透析排班/cloud/藥水小幫手_完整版.gs`（`sendReminders`）

**改完程式碼後，記得：**
1. 重新跑一次，拿真實班表資料驗證，不要只看邏輯覺得「應該對了」就直接上（v3.31 的教訓）
2. 更新這份接續包（鐵律六），而且**如果後來又撤銷了，接續包也要跟著撤銷寫法**（十一節的教訓）
3. 提醒玉繡：重新排這一週之前先「🗑 清除草稿」，避免舊的錯結果被自動套用回來（第九節）
