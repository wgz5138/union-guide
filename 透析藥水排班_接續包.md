# 🔖 透析藥水排班系統 — 接續包

> 給「兩三年後的自己」或「接手的 AI」看：一打開就懂整套怎麼運作、檔案在哪、怎麼改。

## 一、三大組件
| 組件 | 網址 / 位置 | 狀態 |
|---|---|---|
| 手機網頁排班 | https://union-dialysis.streamlit.app/ | `app.py` v2.3 上線中 |
| Google 試算表（當資料庫） | ID: `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js` | 分頁：userid / 對照 / 本週名單 / 排班歷史 / 稽核歷史 |
| Apps Script（LINE 小幫手） | `藥水小幫手_完整版.gs` v3 上線中 | 收 userId、每日提醒、稽核通知、存取歷史 |

## 二、repo 裡的檔案（這個資料夾）
| 檔案 | 版本 | 角色 |
|---|---|---|
| `app.py` | v2.3 | Streamlit 手機網頁。**呼叫同資料夾的 `排班.py`、`稽核.py`** |
| `排班.py` | v0.4 | 每週「印藥水」排班核心。治療日為軸、白班印前1治療日、小夜印前2治療日、自動跳週日/連假、公平輪序、跨區借人 |
| `稽核.py` | v0.2 | 每月「稽核 AK」排班核心。支援 `班型覆蓋.csv`（網頁玉繡確認的白/夜）、雲端歷史學習 |
| `藥水小幫手_完整版.gs` | v3 | 貼到 Apps Script 用 |

> ⚠️ **重點**：`app.py` 用 `os.path.join(HERE, "排班.py")` 找腳本，所以 **`排班.py`、`稽核.py` 一定要跟 `app.py` 在同一個資料夾**。

## 三、設定檔（CSV）— 不必放 repo，第一次跑會自動產生
`排班.py` 啟動時若找不到會自動建立預設檔，之後可用 Excel 編輯：
- `組員名單.csv`（卡號, 姓名）
- `床號分區.csv`（區域類型, 床號, 盡量避開）— 要開第三洗腎室就加「三區」的列
- `不可印班別.csv`（班別）
- `休診日.csv`（日期, 說明）— 過年/國定假日填這
- `排班紀錄.csv` / `稽核紀錄.csv`（程式自動維護的公平歷史）
- `班型覆蓋.csv`（稽核時由網頁寫入：卡號, 姓名, 班型）

## 四、雲端串接（Streamlit Secrets）
在 Streamlit 後台 Settings → Secrets 填：
```toml
APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbz4iRQfQ8dfHf6FJJJc5bUv4uiyrcSKfVPXb621jZs_WFhhCTSpvBYiq7rsMjrPgUj8XA/exec"
WRITE_SECRET = "yaoshui2026"
```
`藥水小幫手_完整版.gs` 裡的 `WRITE_SECRET` 要跟上面**一致**；`LINE_TOKEN` 要貼自己的 Channel access token。

## 五、流程一覽
### 每週印藥水
上傳班表 → 選這一週分頁 → ➡️排印藥水 → 點格子微調 → ✅產生定案 → 🚀送到雲端
→ 試算表「本週名單」更新 → `.gs` 每晚自動 LINE 提醒「明天要印藥水的人」。

### 每月稽核
切到🟩每月稽核 → 選年月 → 上傳班表 → 📊預覽班型 → 確認白/夜 → ✅排稽核
→ 🚀送稽核結果到雲端 → `.gs` LINE 通知每位稽核者本月責任班次。

### 公平輪序（雲端學習）
排班/稽核前先從試算表拉回歷史；送出後把「玉繡實際定案」寫回（`app.py` Fix1），
所以下一輪的公平累計都以真實結果為準。

## 六、想改怎麼辦
跟任何 AI 說：「先看 `透析藥水排班_接續包.md`，然後幫我改 ＿＿＿」即可。
常見：
- 改排班規則 → `排班.py`（`assign()` 三回合邏輯、`is_excluded_shift`、`kind_area`）
- 改稽核規則 → `稽核.py`（`AREAS/GROUPS/SHIFTS`、`assign()`、`classify()`）
- 改網頁畫面/說明 → `app.py`
- 改 LINE 訊息文字 / 提醒時間 → `藥水小幫手_完整版.gs`（`sendReminders` 用 Apps Script 觸發器排每日）
