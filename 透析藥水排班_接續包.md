# 🔖 透析藥水排班系統 — 接續包（現況：v3.37 + gs v5.7，兩邊都已部署上線且已驗證正常）

> **給下一個 AI session**：打開這份文件就知道整套系統怎麼運作、現在在哪個版本、有什麼 bug、
> 有什麼鐵律。**不要問使用者已經在這裡寫過的事情。**
>
> ⚠️ **這份文件在 2026-07-15 大幅重寫過一次**，原因：上一版（2026-07-15 13:26～13:37 寫的）
> 把「剛做完、還沒驗證會不會有副作用」的 v3.31 直接寫成✅定案，結果 90 分鐘後（15:05）
> 那個修法就因為造成雙印回歸被撤銷了，文件卻沒跟著更新。同時上一版對「Streamlit 到底
> 部署哪個檔案」的描述整段是錯的。這次重寫已經逐項跟程式碼、跟玉繡的 Streamlit Cloud
> 後台設定核對過，見第十一節「文件曾經錯在哪」。
>
> ⚠️ **同一天稍晚（v3.32 部署後）又發生第二次事件**：使用者發現「7月統計消失」，AI 一路
> 憑程式碼推論講了好幾次「應該已經同步了」、「理論上會怎樣」，結果每次都被使用者當場戳破
> 是錯的，逼出真正原因（統計頁的「存回雲端」把整份排班歷史洗成假資料）。**這件事的教訓
> 比 v3.31 那次更重要**，見鐵律五之二 + 第十三節「AI 不查證就亂講的完整記錄」。下一個 session
> 不是只要「查證」，是要**先講清楚「這是我驗證過的」還是「這是我猜的」**，兩者不能混著講。

---

## ⚠️ AI 接手鐵律（每次開新 session 必讀、必遵守）

### 鐵律一：玉繡排的班表是最高準則
- **「一切以玉繡排的為最高準則」**。玉繡手動排好、存到 Google Sheets 的結果就是最終答案。
- 演算法排出來的是「建議」，玉繡在 Streamlit 點格子微調後才是定案。
- 不要因為演算法說 A，就否定玉繡說 B。**玉繡說的才對。**

### 鐵律二：不得任意增刪名單
- **「不要因為查修程式、排查而任意增刪名單」**——統計數字會完全出錯。
- 不可以為了測試、驗證、除錯而修改 `排班紀錄.csv`、`稽核紀錄.csv`、`組員名單.csv`。
- 只有玉繡指示才能動名單。

### 鐵律三：設計目的
- **「做這程式是要降低玉繡人為的工作量」**，要以玉繡設的規矩為先做演算，玉繡手動修改為次。
- 努力讓演算法自動產出正確結果，減少玉繡要手動改的次數。

### 鐵律四：URL 格式
- 所有 URL 一律用 `[文字](url)` markdown 格式，絕不貼純文字 URL。

### 鐵律五：未查證不得肯定
- 涉及第三方系統（LINE API、Google、Streamlit Cloud 後台、醫院 IT）一律先說「需要確認」，
  查證後才能肯定。**Streamlit Cloud 的部署設定尤其容易搞錯，務必跟玉繡核對後台設定，
  不要憑接續包舊文字或猜測。**

### 鐵律五之二：「應該／理論上」不能當結論講（2026-07-15 加，因為鐵律五被違反了至少4次才加的）
- **軟化詞不等於誠實揭露不確定性**。講「應該已經同步了」、「理論上會怎樣」、「一般來說會…」
  這種話，使用者沒辦法從字面判斷「這是驗證過的事實」還是「這是讀程式碼猜的」，結果就是
  被當成結論去行動，出錯了才發現根本沒測過。
- **任何沒有親自驗證過（實際跑過、親眼看過畫面截圖、查過即時資料）的判斷，必須明講「這是我
  從程式碼推論的，還沒驗證，需要你確認」**，不能包裝成聽起來有把握的結論。
- **工具連不出去查證不了，就直接說「我查不到，需要你幫忙看」**，不要生一個聽起來合理的猜測
  出來頂著。
- **下一個 session 被抓到講「應該/理論上」卻拿不出驗證證據時，直接承認、去查，不要辯解**。
  這條鐵律本身寫得再細，也不會自動變成 AI 真的會做到——**真正有效的機制是使用者當場戳破，
  不是文件本身**。下一個 session 如果又講「應該」，使用者可以直接問：「這句話你自己驗證過，
  還是讀程式碼猜的？」拿不出證據就是還沒查。完整反面教材見第十三節。

### 鐵律六：接續包必須更新
- 任何 session 新增或改動任何功能，**必須同步更新本檔**。不更新 = 下一個 session 沒上下文。
- **改完程式碼後，如果後來又反悔/撤銷，接續包也要跟著改回去**——不能只更新「做了什麼」，
  忘記更新「後來被撤銷了」。上一版就是栽在這裡（見第十一節）。

---

## 一、三大組件

| 組件 | 位置 / 網址 | 狀態 |
|---|---|---|
| Streamlit 手機排班網頁 | https://union-dialysis.streamlit.app/ | `透析排班/webapp/app.py`（分支 `claude/dialysate-scheduling-rl20mk`）**v3.37** 上線中，git push 自動部署 |
| Google 試算表（當資料庫） | ID: `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js`（試算表本身名稱＝「藥水機器人」） | 分頁：userid / 對照 / 本週名單 / 排班歷史 / 稽核歷史 / 排班草稿 |
| Apps Script（LINE 小幫手） | `透析排班/cloud/藥水小幫手_完整版.gs`（**v5.7**，部署版本19）| 收 userId、每日提醒、稽核通知、存取歷史。✅ 2026-07-19～20 已確認部署網址與 Streamlit Secrets 一致、資料正確落地，見第十五節 |

---

## 二、Git 分支與部署（⚠️ 之前寫錯過，這版已跟玉繡的 Streamlit Cloud 後台核對）

- **Repo**：`wgz5138/union-guide`（公開）
- **真正被 Streamlit Cloud 部署的檔案**：`claude/dialysate-scheduling-rl20mk` 分支的
  **`透析排班/webapp/app.py`**（已直接跟玉繡的 Streamlit Cloud「App settings → Main file path」
  核對過，不是猜的）
- **推到 `claude/dialysate-scheduling-rl20mk` 分支就直接生效，沒有 PR / 合併到 main 這一關**。
  main 分支跟這個 Streamlit app 的部署**完全無關**。
- Worktree 路徑：`/tmp/streamlit-detail`（`git worktree add /tmp/streamlit-detail
  claude/dialysate-scheduling-rl20mk`）。**注意檔案在 worktree 底下的 `透析排班/webapp/`
  子目錄，不是 worktree 根目錄。**

### ⚠️ 陷阱：main 根目錄有一份「沒人在用」的舊複製品
main 分支的**根目錄**也放著一份同名的 `排班.py`、`app.py`、`稽核.py`、`組員名單.csv`。
這份是舊複製品，**不是實際部署的程式**，特徵：
- 沒有 `FORCE_PRINT`／強制可印功能（真正部署的那份有）
- 沒有 `requirements.txt`（Streamlit 部署一定要有，這點就能判斷這份不是入口）
- `組員名單.csv` 姓名還是錯字「余伶緣」，真正部署的那份已經修正為「余怜緣」

**曾經有 session 誤改到這份根目錄複製品，浪費了一輪查證才發現改錯地方。**
下次要改排班/稽核邏輯，**第一件事先確認自己在 `claude/dialysate-scheduling-rl20mk` 分支的
`透析排班/webapp/` 底下**，不是 main 根目錄。（這份舊複製品要不要直接砍掉，待玉繡/小巫決定，
目前先保留但已加這段警示。）

---

## 三、Repo 裡的主要檔案（路徑：`透析排班/webapp/`，分支 `claude/dialysate-scheduling-rl20mk`）

| 檔案 | 版本 | 角色 |
|---|---|---|
| `透析排班/webapp/app.py` | **v3.36** | Streamlit 手機網頁，呼叫 `排班.py`、`稽核.py` |
| `透析排班/webapp/排班.py` | v0.4 基礎，已大幅改版 | 每週印藥水排班核心（見第四節） |
| `透析排班/webapp/稽核.py` | v0.3 | 每月稽核排班 |
| `透析排班/cloud/藥水小幫手_完整版.gs` | v5.4（見第八節）| 貼到 Apps Script，⚠️改完要手動「部署→新版本」才生效，git push 不會自動生效 |
| `透析排班/webapp/組員名單.csv` | — | 15 人卡號+姓名，快速模式用 |
| `透析排班/webapp/排班紀錄.csv` | — | 公平歷史，程式自動維護 ⚠️ 不可手動亂改 |
| `透析排班/webapp/稽核紀錄.csv` | — | 稽核歷史，程式自動維護 ⚠️ 不可手動亂改 |
| `透析排班/webapp/強制可印.csv` | — | 玉繡指定本週放假但仍可印的人（卡號或姓名） |

---

## 四、每週印藥水排班規則（`排班.py`）

### 班表 Excel 格式
- 表頭列：`卡號 | 姓名 | ... | 類別 | 班別 | 責任區域 | （下一天）類別 | 班別 | 責任區域 | ...`
- 程式找「卡號」「姓名」表頭定位；往上幾列找日期
- 責任區域要和 `床號分區.csv` **一字不差**（例如 `1-3,21` / `25-28`）

### 印藥水日期規則
| 班別 | 印的日期 |
|---|---|
| 白班（D 開頭） | `prev_treat(T)`：T 的前一個治療日 |
| 小夜（E 開頭） | `prev_treat(prev_treat(T))`：T 的前兩個治療日 |
- **prev_treat** 自動跳週日 + 休診日（過年/連假填 `休診日.csv`）

### 不能印的班別（`不可印班別.csv` 預設）
```
N、11-7、2-10、3'-12、1'-10、12-8、EP1
副值、收水、對帳、文書、查帳、W103
1線、2線、3線、4線、8-5'、8-4'、1-9
```
- 班表空白（放假不在院）→ 也不能印
- **is_excluded_shift()** 做全字或含字串比對（≥3 字元的 token 用 `in` 比對）

### 資格判斷 kind_area()（**此函式不可更動，已於 2026-07-15 跟現行程式碼逐行核對**）
```python
def kind_area(status, card, d, name=""):
    if d is None: return (None,None)
    rec=status.get(card,{}).get(d)
    if not rec: return (None,None)          # 班表空白（放假不在院）→ 不能印
    cat,shift,zone=rec; s=shift.strip()
    if is_excluded_shift(s): return (None,None)   # 大夜/副值等排除班別 → 不能印
    a=zone_area(zone)
    if a=="未知" and zone.strip():
        UNKNOWN_BEDS.setdefault(zone.strip(),set()).add((name,d)); return (None,None)
    if a in PRINT_AREAS:
        if s.startswith("D"): return ("白",a)
        if s.startswith("E"): return ("夜",a)
    return (None,None)
```

### 床號分區規則
- `床號分區.csv` 欄位：**區域類型, 床號, 盡量避開Y/N**
- 區域類型：`一區` / `二區`（= PRINT_AREAS，可列印）；`排除`（病房/ICU）；`勤務`（非床邊）
- 盡量避開 Y 的床號（22-23, 29-30, 60-63）：排序時排後面，有其他人才輪到他們

### 公平輪序（cost）
- `cost = 印次數 - 休息次數`；負值越大（休越多）越優先排印
- **整週沒印到的人 = 本週「休息」，一律計入公平輪序**，包括病房/ICU/大夜/整週休假
- 公平歷史從 Google Sheets「排班歷史」拉回，`app.py` 送出時同步寫回
- 本週的歷史以 `skip_week=分頁名稱` 排除，避免重算

### 三回合核心邏輯（現況：v3.32，**Round 1+2 已改為全域最佳化配對**）

**Round 1+2（合併）：同區+跨區一起解，全域最佳化配對**

舊版（v3.30 及之前）分兩階段貪心處理：
```python
# 回合1:同區、一人一週一次(舊版，已淘汰)
for (T,A) in sorted(slots, key=lambda s: len(same[s])):   # 候選人數少的格子優先跑
    ...
# 回合2:跨區支援(從組員較多的區借)
for (T,A) in slots:
    ...
```
⚠️ **這個舊寫法有結構性 bug**：候選人數少優先的排序會讓某個治療日搶先處理，把本來該給
「候選人少但日期較早」那格的人搶到別的日子去，造成別人被迫印第二次。2026-07-15 曾經
試過改成「治療日由早到晚」自然順序（v3.31），修好了錯位但反而造成另外兩人（潘冠秀、
余怜緣）雙印，比原本的單一雙印還糟，已撤銷。**教訓：換貪心的處理順序只是把問題挪去
不同人身上，不是真正解決。**

**v3.32 的解法**：不再分兩階段貪心，改用 `scipy.optimize.linear_sum_assignment` 把「同區」
「跨區」候選人一起放進**同一個**指派問題求解，直接算出「本回合能填滿最多格子、且雙印人數
最少」的最佳解，不受格子處理順序影響。優先序透過分層權重編碼進成本矩陣，完全比照舊版：
`is_force`(強制可印) > 同區優先於跨區 > AVOID床 > 公平 cost > 名單順序，**Round 3(印第二次
的規則)完全沒動**，因為那是玉繡已經確認過的規矩，沒有壞就不需要碰。

**驗證方式**（2026-07-15）：拿 3 週真實班表（0629~0705、0706~0712、0713~0719）逐週比對
新舊版本，新版 3 週皆不劣於 v3.30，其中 0713~0719（本次爭議週）從「黃莉婷被迫雙印」改善
為「0人雙印」；也驗證過強制可印不會繞過資格判斷這個安全性質沒有被破壞。另外統計了全部
26 週歷史紀錄，雙印只發生過這 1 次（3.8%）——但這個數字只反映「玉繡手動修正後的最終定案」，
不代表舊演算法本身很少出錯，很可能她過去都在手動擋掉這類問題，只是沒留下紀錄。

⚠️ **這次部署時玉繡剛好在休假，是在她本人不在場的情況下由 AI 判斷後直接部署的**（使用者
明確指示部署）。回來後**務必請她實際用個一兩週看看結果符不符合預期**，特別留意：同一個
人在同一週裡「印哪一天」可能會因為公平分數的些微差異而跟舊版不同（不是 bug，是因為她這週
本來就有不只一天符合資格，選哪天本來就有取捨空間）。

**新增依賴**：`requirements.txt` 加了 `scipy`。

**Round 2 說明併入 Round 1**（舊版說明保留在下方供對照）：
```
跨區借人排序鍵（併入上面的全域配對權重，不再是獨立的第二階段）：
is_force > 同區優先於跨區 > AVOID床 > cost > 名單順序
警告：「※ X區 沒有本區的人 → 從『Y區』借『名字』過來印」
```

**Round 3：還是空 → 印第二次**（Round 1+2 都排不出才跑）
```
每人最多印 2 次（cap）
鐵律：有跨區候選人可印第二次 → 絕不先動同區候選人印第二次
盡量間隔久（gap 越大越優先）；同日一+二也允許
排序鍵：(is_force, AVOID床, 跨區=0>同區=1, -gap, cost, 名單順序)
警告：「※ X 人手不足 → 安排『名字』跨區/同區印第二次(替代方案)」
```

**排不出**：標 ❌，輸出「❌ 完全排不出人！請手動處理」

### 輸出標記
| 標記 | 意義 |
|---|---|
| `(X/Y印)` | 幾月幾日印藥水 |
| `🔺` | 跨區支援 |
| `雙印` | 本週印第二次 |

### 強制可印（FORCE_PRINT，已跟現行程式碼核對，描述正確）
- 玉繡在 `強制可印.csv` 指定某人（放假週但剛好排到合法班別仍可印）
- **效果**：排序優先排他，`is_force` → sort key 0（其他人 1）
- **不改資格**：放假/大夜/空白班依然不能印
- 若指定的人本週完全沒有合法班別，強制可印無效（不會硬塞進去）

---

## 五、每月稽核排班規則（`稽核.py`）（已跟現行程式碼核對，架構描述正確）

### 稽核架構
每月排 **12 個稽核位置**：

| 維度 | 選項 |
|---|---|
| 區域 | 一區、二區（2個） |
| 組別 | W135（週一三五）、W246（週二四六）（2個） |
| 班次 | 第一班、第二班、第三班（3個） |

2 × 2 × 3 = **12 個 slot**

### 班次 vs 班型對應規則
| 班次 | 誰能排 |
|---|---|
| 第一班 | 白班 |
| 第二班 | 白班（優先）；若白班人數 < 夜班人數，小夜也可補 |
| 第三班 | 小夜 |

### 班型自動判斷（第一週小班）
- 從**當月第一週班表**看每人白班（D）/ 小夜（E）天數
- 白班天數 ≥ 小夜天數 → 判為「白班」
- 白 < 夜 → 判為「夜班」
- 完全排除班（大夜等）→ None（無法稽核）
- **玉繡可在 Stage 1 手動覆蓋班型**（網頁點選），優先於自動判斷
- 班型覆蓋寫入 `班型覆蓋.csv`（卡號, 姓名, 班型）

### 區域判斷
- 同樣從**第一週班表**，看責任區域出現最多的是一區還是二區
- 若第一週找不到，改用整月統計

### 三階段排法（稽核 assign()）
```
Round 1：同區、各班次挑最公平的人（cost = 稽核次 - 休息次）
  → 先跑「同區候選人最少的 slot」（讓稀缺格優先被保障）
Round 2：跨區補位（同區排不出才跑）
  → 警告：「※ X區 第N班 本區排不出 → 跨區借『名字』」
排不出：標 ❌，警告「請人工處理」
每月沒稽核到的人 = 本月「休息」，計入公平輪序
```

### 快速模式（免上傳 Excel）
- 網頁玉繡直接點選每人 白班/小夜 與 一區/二區
- 寫入 `快速名冊.csv`（卡號, 姓名, 班型, 區）
- `python 稽核.py --quick 2026-06`
- 輸出格式與 Excel 模式**完全相同**

---

## 六、設定檔（CSV，全部在 `透析排班/webapp/` 底下）

- `組員名單.csv`（卡號, 姓名）— 15 人，快速模式用
- `床號分區.csv`（區域類型, 床號, 盡量避開Y/N）
- `不可印班別.csv`（班別）— 含 N、副值、EP1 等（見第四節完整列表）
- `休診日.csv`（日期, 說明）— 連假/國定假日
- `排班紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `稽核紀錄.csv` — 程式自動維護，**⚠️ 不可手動亂改！統計會出錯**
- `強制可印.csv` — 玉繡本週手動指定（卡號或姓名），每週可清空重寫

---

## 七、歷史統計資料位置（2026-07-15 逐筆重新核對過，修正了一個年份錯誤）

### 資料在哪裡

| 資料 | 主要位置 | 備份位置 |
|---|---|---|
| 印藥水歷史 | Google Sheets「排班歷史」分頁 | `透析排班/webapp/排班紀錄.csv`（branch: `claude/dialysate-scheduling-rl20mk`） |
| 稽核歷史 | Google Sheets「稽核歷史」分頁 | `透析排班/webapp/稽核紀錄.csv`（同上） |

**Google Sheets 才是主要資料來源**。`app.py` 每次開啟時從 GSheets 拉取歷史；送出定案時寫回。CSV 是本機備份。

### 資料量（截至 2026-07-15，實際數過）
- `排班紀錄.csv`：**374 筆**（375 行含表頭），週次從 `0112` 到 `0713~0719`，**逐週推算實際是
  2026-01-12 ～ 2026-07-19**（⚠️ 上一版接續包寫「2025-01-12」是錯的，差一年——26週的資料量
  塞不進「2025年初到2026年中」這麼長的區間，逐週往回推算落點在 2026 年初，已修正）
- `排班紀錄.csv` **有一週資料缺口**：`0706~0712` 這週完全沒有紀錄（`0629` 之後直接跳到
  `0713~0719`），原因未查（可能那週沒送出定案，或漏記）。上一版接續包完全沒提到這個缺口，
  下次做統計/公平輪序分析時要注意這一週是斷的，不是印藥水次數真的是 0。
- `稽核紀錄.csv`：**152 筆**（153行含表頭），從 `114-09`（民國114年9月＝2025-09）到
  `115-07`（民國115年7月＝2026-07），逐月核對無缺口

### CSV 格式
**排班紀錄.csv**（印藥水）：
```
週次, 卡號, 姓名, 狀態, 治療日
0713~0719, 5379, 巫佳容, 印, 週四7/16
0713~0719, G645, 黃怡璇, 休,
```
- 狀態：`印`（本週排到）/ `休`（本週沒排到，含病房/大夜/休假）
- 一整週的人全部都有記錄（印+休合計 = 全組員）
- **「治療日」欄記的是 T（治療日），不是印藥水日**，兩者不是同一天，讀資料時不要搞混

**稽核紀錄.csv**（稽核）：
```
月份, 卡號, 姓名, 狀態, 位置
115-07, 4243, 黃莉婷, 稽核, 二區/週二四六/第二班
115-07, G461, 陳葒萍, 休,
```
- 狀態：`稽核`（本月排到）/ `休`（本月沒排到）

### 新 session 存取方式
新對話可以用以下方式讀取歷史數據：
1. **直接讀 CSV**：`git worktree add /tmp/streamlit-detail claude/dialysate-scheduling-rl20mk`，
   兩個 CSV 在 `/tmp/streamlit-detail/透析排班/webapp/` 底下（**不是 worktree 根目錄**）
2. **從 GSheets 看**：Google Sheets ID `1UF-DjDcrIPDbp016vkIyV9zsLF6Qz5EBo6Bq-z6t-Js`，分頁「排班歷史」和「稽核歷史」

---

## 八、雲端串接（Streamlit Secrets）⚠️ 2026-07-19 發現這節整段舊資訊有問題，已更正

### ⚠️ 血淚教訓：Apps Script 網址曾經對不起來，這不是猜的，是實測發現的

2026-07-19 查「7月統計消失」查到懷疑人生時，才發現一件大事：**Streamlit Secrets 裡設定的
`APPS_SCRIPT_URL`，跟使用者實際打開 Apps Script 編輯器看到、正在部署維護的那個網址，
是兩個不同的部署**！這解釋了那整天「查了半天、修了半天，Sheets 卻完全沒反應」的所有謎團——
因為 AI（包括這個 session 自己）一直在改、在測的 `.gs` 檔案，跟 Streamlit 實際呼叫的網址
根本兜不起來。

**目前確認過的正確網址**（2026-07-19 使用者實際部署後給的）：
```toml
APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbxU0UM3gRmt_I69tWY3whwypJso1O7nhh44igdua3G_Lbi_WQ_hwGNBqkTERim5U3HR/exec"
WRITE_SECRET = "yaoshui2026"
```
**舊版本文件寫的網址（`.../AKfycbz4iRQfQ8dfHf6.../exec`）已確認是錯的或至少是另一個沒在用的
部署，不要再用。** 下一個 session 如果又要查 GAS 相關問題，**第一步先跟使用者確認 Streamlit
Secrets 裡實際設定的網址，跟 Apps Script「管理部署」畫面上顯示的網址是否一致**，不要預設
接續包寫的網址就是對的——這份文件自己就在這件事上騙過人一次。

### `.gs` 檔案版本也曾經天差地遠，git 裡那份長期是過時的

2026-07-19 之前，git 上 `透析排班/cloud/藥水小幫手_完整版.gs` 這份檔案是**很舊的版本**
（提醒邏輯是單層「比對印日期=明天就發」），但實際部署在跑的是 **v5.3**（提醒邏輯完全不同：
「上班日」與「印藥水日」分開算、`prevWorkday_()` 跳過週日/休診日、提前一天+當天兩層提醒、
用「提醒紀錄」分頁防重複發送）。AI 對照 app.py 實際會呼叫的 12 個 action，發現 v5.3 缺少
6 個 app.py 需要的功能（`setWeekDraft`、`getWeekDraft`、`setAllScheduleHistory`、
`getMembers`、`setMembers`、`setAllAuditHistory`），已合併補齊成 **v5.4**（見下方檔案清單），
git 上現在這份才是跟正確網址對應的最新版本，**但仍需要使用者手動貼進 Apps Script 編輯器並
「部署→管理部署→新版本」才會生效**——git push 不會讓 `.gs` 自動生效，這點跟 Streamlit
完全不同，之前的接續包沒有講清楚這件事有多關鍵。

`sendReminders()` 現在的真正邏輯（v5.3/v5.4，不是舊版那個「直接比對印日期」的簡化版）：
- 名單上的「印日期」實際存的是「**上班日**」，真正要印藥水的日子＝上班日的前一個上班日
  （`prevWorkday_()`，跳過週日和 `EXTRA_HOLIDAYS`）
- 兩層提醒：印藥水日的前一個上班日發「🔔提前提醒」，印藥水日當天發「⚠️當天提醒」
- 用「提醒紀錄」分頁記錄「日期｜類型｜姓名｜印日」，同一天同一人同一則不會重複發（防止手動
  測試或鬧鐘多次觸發洗版）

---

## 九、每週操作流程

```
上傳班表（.xls）→ 選分頁 → ➡️ 排印藥水
→ 點格子微調（必要時）→ 強制可印 輸入放假但可印的人
→ ✅ 產生定案 → 🚀 送到雲端
→ 試算表「本週名單」更新 → .gs 每晚 LINE 提醒「明天要印藥水的人」
```

⚠️ **「送到雲端」才是真正更新 Google Sheets**。在 Streamlit 跑演算法、看結果 **不影響** Google Sheets，也不影響 LINE 通知。已送出的定案要改，需在 Streamlit 重新產生後再次送出。

### ⚠️ 重跑本週排班前，兩個「要不要載入舊草稿」的提示都選「不要」——不是找按鈕去清除

`app.py` 有 **3 種不同的「草稿」，容易搞混**：
1. **雲端草稿**（Google Sheets「排班草稿」分頁，給小巫預覽用）——進印藥水模式第一個跳出來的
   提示「📬 偵測到玉繡的排班草稿…」，只有「✅ 載入草稿」/「✕ 略過」兩個按鈕，**沒有清除選項**，
   跟 LINE 通知沒有直接關係。
2. **本機草稿**（`last_week_draft.json`，同裝置重開 app 用）——如果還跳出第二個提示「📂 找到
   上次的定案…」，才有「❌ 不用，重新排」可以清掉。
3. **🗑 清除草稿按鈕**——要先讓 `cloud_rows`（session state）有值（載入過草稿，或已產生過一次
   定案）之後，畫面往下捲才會出現，清的是第1種（雲端草稿分頁），**不影響「送到雲端」實際送出
   的內容**。

**真正關鍵**：`app.py`「產生定案」有一段「自動套回上次定案的修改」邏輯——只要**這次 session 裡
`cloud_rows` 曾經被設過值**（不管是載入了雲端草稿還是本機草稿），且 sheet0 跟上次一樣，就會
自動把舊資料的每一格覆蓋回新算出來的結果，目的是保留玉繡手動改過的地方（例如「顏凰任→張雅雯」）
不用每次重改。**但這個邏輯分不出「這是玉繡手動改的」還是「這是上一次程式還有 bug 時算出來的錯
結果」**——連程式自己的註解都寫「同日多人時仍可能偏差」。「🚀 送到雲端」送出去的是**當下 session
裡的那份 `rows`**，跟雲端草稿分頁乾不乾淨無關，所以「清除雲端草稿」本身並不能解決問題。

**2026-07-15 已經證實會出事一次**：程式今天中午一度算錯（v3.31 那次），錯的結果被存成草稿；
後來雖然程式碼改回正確版本，但重開 app 時「自動套回上次定案」把舊的錯結果蓋回來，導致玉繡
送到雲端、LINE 也真的通知了錯的日期。

**所以正確做法是**：只要今天程式碼被改過（不管是修 bug 還是玉繡自己調規則），重新排這一週時，
**兩個「要不要載入舊草稿」的提示都選「不要」**（第一個按「✕ 略過」，第二個如果出現就按
「❌ 不用，重新排」）——這樣 `cloud_rows` 全程不會被舊資料污染，接著正常「上傳/選這週 →
排印藥水 → 產生定案」，仔細核對格子內容，確認沒問題再「送到雲端」。

---

## 十、已知待處理事項

1. **玉繡回來後要確認 v3.32 演算法實際用起來 OK**：Round 1 排序 bug 已於 2026-07-15 用全域
   最佳化配對修復並部署（見第四節），玉繡休假中，是使用者代為指示部署的。回來後請她用個
   一兩週看看格子跟預期符不符合，尤其是「同一人印哪一天」在有多天資格時可能跟舊版不同。

2. **main 根目錄的舊複製品要不要清掉**：待玉繡/小巫決定是否直接刪除，目前先在文件加警示（見
   第二節）避免下次又改錯地方。

3. ✅ **已解決（2026-07-15）**：`0706~0712` 這週排班紀錄缺失，已對照玉繡實際定案的印藥水
   名單截圖、配合當週真實班表算出每人正確治療日，補上 15 筆並送到雲端（見第十三節完整過程）。
   **最可能原因**：這週剛好落在 2026-07-05~07-11 程式密集改版（v3.14~v3.25，短短一週十幾個
   版本）期間，很可能當時「送到雲端」時，LINE通知（setWeek）成功了但排班歷史（push_history）
   悄悄失敗——跟這次 7月事件是**同一個 bug**，只是更早發生、沒被發現。這個 bug 本身已在
   v3.33/v3.34 修掉（失敗會顯示、成功也顯示筆數），理論上以後不會再無聲漏一整週——但這句話
   本身也只是推論，不是驗證過的保證（鐵律五之二）。

4. ✅ **已解決（2026-07-15）**：0713~0719 這週的資料已修正並送到雲端。本次事件的根本原因是
   「自動套回上次定案」把今天中午算錯的舊草稿蓋回新結果（見第九節）。處理方式：
   - git 上的備份 `排班紀錄.csv` 已直接改成玉繡確認過的實際定案（顏凰任週六7/18、黃怡璇
     週一7/20跨區、黃莉婷不再雙印，見下方確認過的正確版本）
   - 使用者透過 Streamlit（略過兩個舊草稿提示 → 重排 → **手動點格子逐一改成跟玉繡確認的
     實際安排一致**，因為 v3.32 的配對結果跟玉繡手動微調過的版本在好幾格上不一樣，不是
     只有 3 格差異 → 產生定案 → 送到雲端）已把正確版本送到 Google Sheets，本週名單與排班
     歷史都已同步更新。

   **0713~0719 玉繡確認過的實際定案**（供對照，日期為印藥水日）：
   ```
   一區：潘冠秀(7/13) 陳葒萍(7/13) 林欣儀(7/14) 張雅雯(7/16) 顏凰任(7/17) 黃怡璇(7/17)🔺
   二區：劉于甄(7/13) 王乃君(7/13) 巫佳容(7/14) 黃莉婷(7/15) 余怜緣(7/17) 高翠盈(7/17)
   休息：于淑鳳、廖緗玗、邱玉繡
   ```

5. ✅ **已解決（2026-07-20 凌晨）**：`APPS_SCRIPT_URL` 部署網址不同步事件（見第八節）。使用者
   已確認 Streamlit Secrets 就是那個正確網址，且用 v5.7 回傳的 `ssId`/`ssName` 診斷資訊確認
   Apps Script 寫入的就是使用者實際在看的那份試算表。完整過程見第十五節。

6. ✅ **已解決（2026-07-20 凌晨）**：`0720~0726` 這週的排班歷史，之前使用者一直看不到新資料，
   查到最後發現**不是程式沒寫入，是使用者看的 Google Sheets 分頁沒重新整理（F5）**——資料其實
   早就正確寫進去了。F5 後第 391~405 列確認 15 筆資料正確（含潘冠秀/黃怡璇/王乃君休息，
   跟玉繡確認過的版本一致）。完整診斷過程見第十五節，**下一個 session 遇到「畫面顯示成功但
   Google Sheets 好像沒反應」，第一步先請使用者對著 Google Sheets 本尊按 F5 硬性重新整理，
   不要急著懷疑程式碼**。

7. ⚠️ **待玉繡/使用者確認**：2026-07-19 拿玉繡手寫的休息紀錄跟系統資料逐人核對（15人全部
   核對過，見第十四節），發現兩處落差：
   - `0609` 這週的週次代碼很可能是打錯的（應該是 `0608`，證據：系統裡其他週次都是乾淨的
     每週一循環，只有這週的日期是週二，且玉繡手寫紀錄也是寫 6/8 不是 6/9）——**還沒動，
     等使用者確認才能改**（鐵律二）
   - `0518` vs 玉繡寫的 `5/17`：證據不像上面那條強（5/17是週日，不是正常的週次錨點），
     可能是玉繡手寫時記憶有落差，不一定是系統錯——**先不動，待確認**

---

## 十一、文件曾經錯在哪（2026-07-15 稽核記錄，避免同樣的錯再發生一次）

上一版接續包（2026-07-15 13:26 與 13:37 兩次 commit）出的問題，具體如下：

1. **把「剛做完、還沒驗證」的修法寫成✅定案**：v3.31（12:59 做的）在 13:26/13:37 就被寫成
   最終正確版本，但 15:05 就因為造成雙印回歸被撤銷了，文件沒有跟著改回來。**教訓：剛做完的
   修法，至少要等玉繡實際跑過、確認沒有新的副作用，才能在文件裡寫✅。**

2. **部署架構整段憑印象/猜測寫錯**：舊版寫「Streamlit 從 main 分支根目錄的 app.py 部署，
   推到 main 才生效，開發先在分支改、PR 合併後生效」，但實際上 Streamlit Cloud 的 Main file
   path 設定的是 `claude/dialysate-scheduling-rl20mk` 分支的 `透析排班/webapp/app.py`，推那個
   分支就直接生效，**根本沒有 PR 合併這一步**。**教訓：部署方式這種事屬於鐵律五「第三方系統
   未查證不得肯定」，要嘛去看 Streamlit Cloud 後台實際設定，要嘛直接問玉繡/小巫，不能憑舊文件
   或猜測寫。**

3. **檔案清單沒寫路徑，害人改錯檔案**：舊版檔案清單只寫檔名沒寫路徑，導致真的有 session 跑去
   改 main 根目錄那份沒人用的舊複製品。**教訓：檔案清單一定要連分支、路徑一起寫死。**

4. **統計數據年份寫錯**：`排班紀錄.csv` 374 筆的起始日期舊版寫「2025-01-12」，實際逐週推算是
   **2026-01-12**（差一年）。**教訓：統計數據要嘛實際數過/推算過，要嘛註明「未核對」，不能照抄
   一個沒驗證過的日期。**

5. **`0706~0712` 資料缺口完全沒提到**：這不是「錯」，是「漏」——舊版統計數據只講了「有多少筆、
   從哪到哪」，沒有實際逐週檢查中間有沒有斷。

---

## 十二、想改怎麼辦

跟任何 AI 說：「先看 `透析藥水排班_接續包.md`，然後幫我改 ___」即可。

**但務必提醒 AI：真正要改的程式在 `claude/dialysate-scheduling-rl20mk` 分支的
`透析排班/webapp/` 底下，不是 main 根目錄同名檔案（見第二節陷阱說明）。**

常見改動位置：
- 排班規則 → `透析排班/webapp/排班.py` 的 `assign()` 三回合邏輯
- 資格判斷 → `透析排班/webapp/排班.py` 的 `kind_area()`、`is_excluded_shift()`
- 強制可印 → `透析排班/webapp/排班.py` 的 `FORCE_PRINT` 全域集合、`is_force()`
- 網頁畫面 → `透析排班/webapp/app.py`
- LINE 訊息文字/提醒時間 → `透析排班/cloud/藥水小幫手_完整版.gs`（`sendReminders`）

**改完程式碼後，記得：**
1. 重新跑一次，拿真實班表資料驗證，不要只看邏輯覺得「應該對了」就直接上（v3.31 的教訓）
2. 更新這份接續包（鐵律六），而且**如果後來又撤銷了，接續包也要跟著撤銷寫法**（十一節的教訓）
3. 提醒玉繡：重新排這一週時，兩個「要不要載入舊草稿」的提示都選不要，避免舊的錯結果被自動套用回來（第九節）

---

## 十三、2026-07-15「7月統計消失」事件全記錄（鐵律五之二的由來）

v3.32 部署當天稍晚，使用者發現統計頁看不到 7 月資料，一路追查到最後才找到真相。過程中 AI
（這個 session）**至少 4 次講出「應該／理論上」卻沒有驗證，被使用者當場戳破**，逼出了鐵律
五之二。完整記錄如下，故意寫得很白，給下一個 session 當警惕。

### AI 講錯話的 4 個具體案例

1. **「送到雲端」的兩步驟被我講成「理論上應該都成功了」**：`app.py` 的「🚀 送到雲端」其實是
   `setWeek`（LINE用）+ `push_history`（統計用）兩個獨立請求，我讀完程式碼、看到「已同步374筆」
   的成功訊息，就直接說「理論上排班歷史應該已經同步了」——**根本沒有實際去讀那份資料驗證**，
   使用者截圖給我看還是舊數字，我才發現講錯。

2. **猜測「0706~0712 可能沒送出過」，被 LINE 通知的事實打臉**：我一度講「這週大概沒人成功
   送到雲端」，但使用者反問「LINE都有通知，怎麼可能沒送？」——這個反問是對的，`setWeek`
   確實成功過（不然不會有 LINE 通知），錯的是 `push_history`，我把兩個不同的請求混為一談。

3. **「清除草稿」按鈕的操作指引跟使用者實際看到的畫面對不上**：我用 `grep` 找到函式名稱就
   直接講位置，沒有把整段 UI 流程讀完，講出來的操作步驟跟使用者實際畫面的「載入/略過」兩個
   按鈕對不起來，使用者照做找不到，回頭問我才發現我講錯。

4. **多次講「應該已經同步了」，實際上排班歷史被整個洗掉**：最後發現真正原因是「統計管理→
   印藥水統計」分頁可以直接改印次/休次數字、按「存回雲端」會呼叫 `setAllScheduleHistory`，
   GAS 端整份 `clearContents()` 重建，把全部人的每週明細換成「統計-印01」這種沒有日期的假列。
   這件事發生在**我開始查證之前**，但我前面幾輪的「應該已經同步」讓使用者一直在錯誤的方向
   （網路失敗、GAS部署版本）反覆確認，沒有及早往「資料本身被覆蓋」這個方向查。

### 根本原因與修復

- **`push_history()` 的靜默失敗**：`app.py` 裡「排班歷史」同步失敗時原本完全不會提示（`if ok2:
  ...` 沒有 `else`），畫面只顯示「🎉 已送到雲端」的成功訊息（那是 `setWeek` 那一步的訊息）。
  v3.33 加了失敗提示；v3.34 進一步發現「GAS 端 `setScheduleHistory_()` 不驗證 rows 內容，收到
  空陣列也回 `{ok:true}`」，所以就算失敗也不一定會跳錯誤，改成 `push_history()` 回傳
  `(ok, 實際筆數, 失敗原因)`，**成功也顯示筆數**，才能抓到「回報成功但其實0筆」這種情況。
- **統計頁「存回雲端」整份洗掉歷史**：v3.35 加了防呆——這顆按鈕現在要先勾選「我知道會清空
  每週明細」才能按，並在上方加醒目警告。
- **事後排查發現**：`push_week_draft`（雲端草稿）早在 2026-07-11（v3.24「四項系統性bug修正」）
  就修過一模一樣的靜默失敗問題，但**只修了這一個函式，沒有推廣去檢查其他結構相同的函式**，
  導致 `push_history` 這個孿生 bug漏到今天才被抓到。事後把全部 9 個雲端寫入函式都檢查過一輪，
  確認現在只有這一類問題、沒有其他遺漏。

### 資料復原結果

- `0713~0719`：已用玉繡確認過的實際定案覆蓋（見第十節第4項）
- `0706~0712`：對照玉繡的印藥水名單截圖 + 當週真實班表，逐人算出正確治療日，補回 15 筆
  （一區：潘冠秀🔺週二7/7、顏凰任週三7/8、陳葒萍週四7/9、林欣儀週五7/10、王乃君🔺週六7/11、
  張雅雯週一7/13；二區：劉于甄週二7/7、巫佳容週三7/8、黃怡璇週四7/9、高翠盈週五7/10、
  邱玉繡週六7/11、廖緗玗週一7/13；休息：于淑鳳、黃莉婷、余怜緣）
- `排班紀錄.csv` 從 374 筆／26週 變成 **389 筆／27週**
- 已同步到 Google Sheets「排班歷史」（透過「📤 同步本機CSV→雲端」）

### 給下一個 session 的具體建議

- 統計數字兜不起來時，**第一步不是猜網路問題，是先確認「寫入」跟「讀取」是不是同一套資料**
  ——這次卡最久的地方，就是我一直在「送到雲端有沒有成功」打轉，沒想到資料是被另一個完全
  不相關的功能（統計頁手動編輯）整個蓋掉的。
- 如果環境連不出外部網路（這次就連不到 Streamlit Cloud 和 Google Apps Script，proxy 回 403），
  **直接承認查不到，請使用者本人去看 Google Sheets 原始表格**，比在程式碼裡猜半天有效率
  太多——這次就是請使用者直接開 Sheets 看最後一列，一次就看出寫入其實有成功，只是讀取端
  出問題。

---

## 十四、2026-07-19「Apps Script 網址對不起來」事件 + 玉繡手寫紀錄核對記錄

### 事件經過（比第十三節更嚴重的一次教訓）

第十三節那次事件修完（v3.33~v3.36）之後，使用者實際去按「送到雲端」，這次**連 v3.36 修的
NaN 錯誤都沒跳了**，畫面顯示「🎉已送到雲端！共12筆」「✅排班歷史已同步（15筆）」，看起來完全
成功。但使用者直接開 Google Sheets 本尊核對，最後一列還是停在 `0713~0719`，新的一週完全沒
進去——這次連錯誤訊息都沒有，是「看起來全部正常，資料卻沒有」。

一路往下查才發現：**Streamlit Secrets 裡的 `APPS_SCRIPT_URL`，跟使用者實際在維護的 Apps
Script 部署網址，根本不是同一個**。AI（這個 session）從對話一開始就把 git 上那份 `.gs`
檔案當成「唯一版本」在讀、在改、在傳給使用者「照抄部署」——結果那份根本是舊到跟兩邊都對不上
的版本（連 `sendReminders()` 的核心邏輯都不一樣：舊版是單層「印日期=明天就發」，真正在跑的
v5.3 是「上班日/印藥水日分開算＋兩層提醒＋防重複」）。**差點因為 AI 的錯誤指示，讓使用者把
正確的 v5.3 蓋成錯誤的舊版。**

教訓：**接續包裡記的任何網址、任何「這就是唯一版本」的檔案，都屬於鐵律五要查證的範圍**，
不能因為「git 裡只有一份、看起來很像正式版」就預設它是對的。Apps Script 這類「檔案存在 git
但實際生效與否要看有沒有手動部署」的系統，尤其容易跟現實脫節。

### 修復

- 比對 app.py 實際會呼叫的全部 12 個 GAS action，找出 v5.3 缺少的 6 個（`setWeekDraft`、
  `getWeekDraft`、`setAllScheduleHistory`、`getMembers`、`setMembers`、`setAllAuditHistory`），
  從舊版 port 過來，補成 v5.4，**v5.3 原本的提醒邏輯完全沒動**
- 使用者已手動部署到正確網址：`https://script.google.com/macros/s/AKfycbxU0UM3gRmt_I69tWY3whwypJso1O7nhh44igdua3G_Lbi_WQ_hwGNBqkTERim5U3HR/exec`
- ✅ **2026-07-20 已確認** Streamlit Secrets 就是這個網址，且資料正確落地，見第十五節

### 玉繡手寫休息紀錄核對（115.1/11 歸零重新統計）

使用者拿玉繡手寫的 15 人休息日期紀錄（`115.1/11`＝2026-01-11 歸零重新統計；顏凰任 115.5月
回歸藥水組，故資料量遠少於其他人），AI 拿系統裡的 389 筆真實資料逐人比對（不是用眼睛看）：

- **8人完全一致**：余怜緣、巫佳容、黃莉婷、于淑鳳、廖緗玗、邱玉繡、張雅雯、顏凰任
- **3人只差在 7/20**（潘冠秀、黃怡璇、王乃君）：不是錯誤，就是當時還沒同步成功的新一週，
  已於 2026-07-20 確認同步成功且資料正確（見第十五節）
- **3人「0609」對不上**（高翠盈、劉于甄、陳葒萍）：系統記錄是「0609」（週二），但其他所有
  週次都是乾淨的每週一循環，玉繡手寫也是記 6/8——**很可能是資料輸入時打錯，0608 打成 0609**，
  待使用者確認後修正（鐵律二，不能自己動）
- **「0518」vs 玉繡的「5/17」**（高翠盈、林欣儀、王乃君）：5/17 是週日，不是正常錨點，證據
  不像上一條強，可能是玉繡手寫時的記憶落差，先不動待確認

這次核對意外證實：**系統資料整體是可信的**（15人裡只有2個具體週次有疑義，其餘完全對上），
不是因為之前查過的一堆 bug 就代表資料全部不可信——查 bug 跟查資料正確性是兩件事，不要因為
查到很多 bug 就對所有資料喪失信心，該用實際核對取代焦慮。

---

## 十五、2026-07-19深夜～07-20凌晨「回報成功但Sheets沒新資料」事件（最終解謎：不是bug）

### 背景

第十四節網址問題修好、v5.4部署之後，使用者按「🚀 送到雲端」，畫面顯示兩個都成功
（「🎉已送到雲端！共12筆」+「✅排班歷史已同步（15筆）」），Apps Script「執行項目」也顯示
`doPost` 正常完成、沒有拋出例外、版本也對——但使用者直接開 Google Sheets 本尊核對，最後一列
還是停在 `0713~0719`，新的一週完全沒進去。這次連「執行紀錄」這個唯一能查的線索都顯示一切正常，
純粹從外部行為完全看不出問題出在哪。中間也先處理了一個**真的存在**的問題：389列逐列
`appendRow()` 寫入太慢，同步過大量資料（例如整份 `setAllScheduleHistory`）會逾時，改成
`writeAllRowsFast_()`（`Range.setValues()` 一次寫入整批）解決，這是 **v5.5**，但解決完
還是沒解開「這週資料進不去」的謎。

### 診斷手法（逐步逼近，過程如實記錄，不是一次就找到答案）

1. **v5.6**：在 `setScheduleHistory` handler 加 `Logger.log()`，記錄收到的 `key`/`rows`筆數/
   第一筆內容，以及最關鍵的 `ss.getId()`/`ss.getName()`（驗證「這支部署綁定的到底是不是
   使用者在看的那份 Sheets」這個懷疑）＋寫入前後列數。**結果：使用者回報「執行項目」畫面上
   那幾筆 `doPost` 記錄「無法展開」，Cloud 記錄檔點不出內容**——UI 本身有障礙，這條路走不通，
   不代表程式有問題。
2. **v5.7**：改弦易轍，不靠 Apps Script 的記錄檔 UI，直接把 `ss.getId()`/`ss.getName()`/
   寫入前後列數/GAS實際收到的rows筆數與key，全部塞進 `setScheduleHistory` 回傳的 JSON
   本身；app.py `push_history()` 同步解析並顯示在「✅ 排班歷史已同步」訊息下方新增的
   「🔍 診斷：」那一行——不用再靠使用者去點開任何後台 UI，診斷資訊直接顯示在操作畫面上。
3. 使用者重新部署 v5.7（版本19）並重新操作一次，畫面出現：
   ```
   🔍 診斷：GAS回報：寫入前405列→寫入後405列；GAS收到15筆(送出15筆) key='0720~0726'；
   寫入試算表: 藥水機器人 (ID尾碼…q-z6t-Js)
   ```
   - `ID尾碼…q-z6t-Js` 跟使用者原本開著的那份 Sheets（`...Bq-z6t-Js`）**完全對上**——不是
     綁錯試算表。
   - 「寫入前405列」本身就是關鍵線索：使用者上次直接核對 Google Sheets 原始畫面時只看到
     390列（最後一筆 `0713~0719`），但這次寫入之前就已經是405列——代表**寫入其實在使用者
     核對之後就已經成功過一次了**，這次只是把同一週的15筆資料覆蓋重寫一次（先移除舊的15筆
     這週資料、再補回新的15筆，列數打平）。

### 真正的結論（使用者親自 F5 重新整理 Google Sheets 驗證，不是推論）

叫使用者直接對著 Google Sheets 分頁按 **F5 強制重新整理**（不是切分頁回去看，是真的重新整理），
結果第391~405列**清清楚楚出現 `0720~0726` 這週的15筆資料**，且潘冠秀/黃怡璇/王乃君休息，
跟玉繡確認過的正確版一致。

**最終判定：這從頭到尾都不是程式的 bug，是使用者那個 Google Sheets 分頁的畫面沒有重新整理，
一直在看舊的快取畫面**，跟 CLAUDE.md 常見問題裡「使用者裝置卡在舊版畫面」是同一種模式，只是
這次發生在 Google Sheets 本身而不是 union-guide 網站。v5.5/v5.6/v5.7 這三版的程式碼改動
（批次寫入、診斷資訊）都是有意義的強化，**但都不是這次「看不到資料」問題的真正解方**——
真正解方是「按 F5」。下一個 session 看到「畫面顯示成功、但 Google Sheets 好像沒反應」這種
回報，第一步永遠是先確認使用者有沒有重新整理過 Google Sheets 分頁，這比懷疑程式碼便宜太多。

### 目前線上版本（已部署、已驗證，不是待辦）

- Streamlit：`app.py` **v3.37**，git分支 `claude/dialysate-scheduling-rl20mk`，push即生效
- Apps Script：`藥水小幫手_完整版.gs` **v5.7**，部署版本19，網址
  `https://script.google.com/macros/s/AKfycbxU0UM3gRmt_I69tWY3whwypJso1O7nhh44igdua3G_Lbi_WQ_hwGNBqkTERim5U3HR/exec`
- `排班紀錄.csv` 405列，最新一週 `0720~0726` 已確認資料正確
- 下一個 session **不需要再懷疑這條同步流程本身有問題**，除非有新的具體失敗現象
